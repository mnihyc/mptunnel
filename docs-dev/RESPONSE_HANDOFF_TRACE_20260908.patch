diff --git a/src/runtime/path/tcp/client/connection.rs b/src/runtime/path/tcp/client/connection.rs
index 1686b83..d25041f 100644
--- a/src/runtime/path/tcp/client/connection.rs
+++ b/src/runtime/path/tcp/client/connection.rs
@@ -226,6 +226,27 @@ pub(in crate::runtime) async fn connect_client_tcp_carrier(
             reader,
             reliable_path_writer_frame_queue(mux_limits),
             move |frame| {
+                #[cfg(feature = "lab-diagnostics")]
+                if let Frame::StreamData {
+                    stream_id,
+                    offset,
+                    payload,
+                } = frame
+                {
+                    crate::lab_diagnostics::lab_diagnostic(
+                        "response_tcp_handoff",
+                        format_args!(
+                            "role=client phase=authenticated_decode session_id={} path_index={} remote_port={} stream_id={} offset={} end={} len={}",
+                            session_id.0,
+                            path_id.0,
+                            remote_port,
+                            stream_id.0,
+                            offset,
+                            offset.saturating_add(payload.len() as u64),
+                            payload.len(),
+                        ),
+                    );
+                }
                 if let Frame::SessionClose { reason } = frame {
                     observed_lifecycle.retire(*reason);
                 }
diff --git a/src/runtime/path/tcp/client/stream.rs b/src/runtime/path/tcp/client/stream.rs
index 8118f43..6f2cdef 100644
--- a/src/runtime/path/tcp/client/stream.rs
+++ b/src/runtime/path/tcp/client/stream.rs
@@ -477,7 +477,23 @@ pub(in crate::runtime::path::tcp) async fn handle_client_tcp_stream_frame(
             offset,
             payload,
         } => {
-            route_client_tcp_stream_frame(
+            #[cfg(feature = "lab-diagnostics")]
+            let response_len = payload.len();
+            #[cfg(feature = "lab-diagnostics")]
+            lab_diagnostic(
+                "response_tcp_handoff",
+                format_args!(
+                    "role=client phase=route_begin mode=actor path_index={} path_instance_id={} stream_id={} offset={} end={} len={} mailbox_free={:?}",
+                    runtime.path_index,
+                    connection.path_instance_id.as_u64(),
+                    stream_id.0,
+                    offset,
+                    offset.saturating_add(response_len as u64),
+                    response_len,
+                    streams.get(&stream_id).map(|state| state.frames.capacity()),
+                ),
+            );
+            let result = route_client_tcp_stream_frame(
                 streams,
                 closed_streams,
                 stream_id,
@@ -487,7 +503,23 @@ pub(in crate::runtime::path::tcp) async fn handle_client_tcp_stream_frame(
                     payload,
                 },
             )
-            .await
+            .await;
+            #[cfg(feature = "lab-diagnostics")]
+            lab_diagnostic(
+                "response_tcp_handoff",
+                format_args!(
+                    "role=client phase=route_end mode=actor path_index={} path_instance_id={} stream_id={} offset={} end={} len={} attachment_present={} result={}",
+                    runtime.path_index,
+                    connection.path_instance_id.as_u64(),
+                    stream_id.0,
+                    offset,
+                    offset.saturating_add(response_len as u64),
+                    response_len,
+                    streams.contains_key(&stream_id),
+                    if result.is_ok() { "ok" } else { "error" },
+                ),
+            );
+            result
         }
         Frame::StreamAck {
             stream_id,
diff --git a/src/runtime/path/tcp/client/writer.rs b/src/runtime/path/tcp/client/writer.rs
index b67c3ee..d381668 100644
--- a/src/runtime/path/tcp/client/writer.rs
+++ b/src/runtime/path/tcp/client/writer.rs
@@ -814,7 +814,54 @@ fn try_route_client_tcp_frame_during_write_for_session(
             Err(error) => Err(error),
         };
     }
-    try_route_client_tcp_frame_during_write(frame, streams, closed_streams, datagrams)
+    #[cfg(feature = "lab-diagnostics")]
+    let response = match &frame {
+        Frame::StreamData {
+            stream_id,
+            offset,
+            payload,
+        } => {
+            lab_diagnostic(
+                "response_tcp_handoff",
+                format_args!(
+                    "role=client phase=route_begin mode=interlocked path_index={} path_instance_id={} stream_id={} offset={} end={} len={} mailbox_free={:?}",
+                    runtime.path_index,
+                    path_instance_id.as_u64(),
+                    stream_id.0,
+                    offset,
+                    offset.saturating_add(payload.len() as u64),
+                    payload.len(),
+                    streams.get(stream_id).map(|state| state.frames.capacity()),
+                ),
+            );
+            Some((*stream_id, *offset, payload.len()))
+        }
+        _ => None,
+    };
+    let result = try_route_client_tcp_frame_during_write(frame, streams, closed_streams, datagrams);
+    #[cfg(feature = "lab-diagnostics")]
+    if let Some((stream_id, offset, len)) = response {
+        lab_diagnostic(
+            "response_tcp_handoff",
+            format_args!(
+                "role=client phase=route_result mode=interlocked path_index={} path_instance_id={} stream_id={} offset={} end={} len={} attachment_present={} result={}",
+                runtime.path_index,
+                path_instance_id.as_u64(),
+                stream_id.0,
+                offset,
+                offset.saturating_add(len as u64),
+                len,
+                streams.contains_key(&stream_id),
+                match &result {
+                    Ok(ClientTcpWriteFrameRoute::Routed) => "routed_or_retired",
+                    Ok(ClientTcpWriteFrameRoute::Mailbox { .. }) => "mailbox_full_pending",
+                    Ok(ClientTcpWriteFrameRoute::Barrier(_)) => "barrier",
+                    Err(_) => "error",
+                },
+            ),
+        );
+    }
+    result
 }
 
 fn try_route_client_tcp_frame_during_write(
diff --git a/src/runtime/path/tcp/server/session.rs b/src/runtime/path/tcp/server/session.rs
index ef0c336..98eb39e 100644
--- a/src/runtime/path/tcp/server/session.rs
+++ b/src/runtime/path/tcp/server/session.rs
@@ -782,6 +782,28 @@ impl ServerTcpPathSession {
                     ));
                 }
                 ReliablePathCommand::SendFrame(frame) => {
+                    #[cfg(feature = "lab-diagnostics")]
+                    if let Frame::StreamData {
+                        stream_id,
+                        offset,
+                        payload,
+                    } = &frame
+                    {
+                        lab_diagnostic(
+                            "response_tcp_handoff",
+                            format_args!(
+                                "role=server phase=command_stage session_id={} path_index={} path_instance_id={} stream_id={} offset={} end={} len={} writer_pending_bytes={}",
+                                self.path_registration.session_id().0,
+                                self.path_id.0,
+                                self.path_registration.path_instance_id().as_u64(),
+                                stream_id.0,
+                                offset,
+                                offset.saturating_add(payload.len() as u64),
+                                payload.len(),
+                                writer_pending_bytes,
+                            ),
+                        );
+                    }
                     writer_pending_bytes = writer_pending_bytes.checked_add(pending_bytes).ok_or(
                         RuntimeError::Protocol("server TCP writer transaction byte overflow"),
                     )?;
@@ -949,7 +971,18 @@ impl ServerTcpPathSession {
                 .commit_transaction_interlocked(writer_pending_bytes)
                 .await;
         }
-        let wrote = self.writer.commit_transaction(&mut self.evidence).await?;
+        let wrote = self
+            .writer
+            .commit_transaction(
+                &mut self.evidence,
+                #[cfg(feature = "lab-diagnostics")]
+                (
+                    self.path_registration.session_id().0,
+                    self.path_id.0 as usize,
+                    self.path_registration.path_instance_id().as_u64(),
+                ),
+            )
+            .await?;
         if wrote && *writer_pending_bytes > 0 {
             self.evidence
                 .observe_after_write(&self.context, &self.path_registration, self.path_id);
@@ -973,7 +1006,15 @@ impl ServerTcpPathSession {
         let mut routed_frames = 0usize;
         let mut mailbox: Option<PendingMailboxFrame> = None;
         let write_result = {
-            let write = self.writer.commit_transaction(&mut self.evidence);
+            let write = self.writer.commit_transaction(
+                &mut self.evidence,
+                #[cfg(feature = "lab-diagnostics")]
+                (
+                    self.path_registration.session_id().0,
+                    self.path_id.0 as usize,
+                    self.path_registration.path_instance_id().as_u64(),
+                ),
+            );
             tokio::pin!(write);
             loop {
                 tokio::select! {
diff --git a/src/runtime/path/tcp/server/writer.rs b/src/runtime/path/tcp/server/writer.rs
index 0ccb601..3a418a3 100644
--- a/src/runtime/path/tcp/server/writer.rs
+++ b/src/runtime/path/tcp/server/writer.rs
@@ -53,15 +53,24 @@ impl ServerTcpWriter {
     pub(in crate::runtime::path::tcp) async fn commit_transaction(
         &mut self,
         evidence: &mut ServerTcpEvidenceState,
+        #[cfg(feature = "lab-diagnostics")] diagnostic_identity: (u64, usize, u64),
     ) -> Result<bool, RuntimeError> {
         if self.pending_frames.is_empty() {
             return Ok(true);
         }
+        #[cfg(feature = "lab-diagnostics")]
+        self.observe_response_transaction("write_begin", diagnostic_identity, "pending");
         let commit = async {
             self.framed.write_frames(&self.pending_frames).await?;
             self.framed.flush().await
         }
         .await;
+        #[cfg(feature = "lab-diagnostics")]
+        self.observe_response_transaction(
+            "flush_end",
+            diagnostic_identity,
+            if commit.is_ok() { "ok" } else { "error" },
+        );
         match commit {
             Ok(()) => {
                 for frame in &self.pending_frames {
@@ -75,6 +84,39 @@ impl ServerTcpWriter {
         }
     }
 
+    #[cfg(feature = "lab-diagnostics")]
+    fn observe_response_transaction(
+        &self,
+        phase: &str,
+        (session_id, path_index, path_instance_id): (u64, usize, u64),
+        result: &str,
+    ) {
+        for frame in &self.pending_frames {
+            if let Frame::StreamData {
+                stream_id,
+                offset,
+                payload,
+            } = frame
+            {
+                crate::lab_diagnostics::lab_diagnostic(
+                    "response_tcp_handoff",
+                    format_args!(
+                        "role=server phase={} session_id={} path_index={} path_instance_id={} stream_id={} offset={} end={} len={} result={}",
+                        phase,
+                        session_id,
+                        path_index,
+                        path_instance_id,
+                        stream_id.0,
+                        offset,
+                        offset.saturating_add(payload.len() as u64),
+                        payload.len(),
+                        result,
+                    ),
+                );
+            }
+        }
+    }
+
     pub(in crate::runtime::path::tcp) async fn write_frame(
         &mut self,
         frame: &Frame,
diff --git a/src/runtime/relay/client.rs b/src/runtime/relay/client.rs
index 9c37ad6..6daf955 100644
--- a/src/runtime/relay/client.rs
+++ b/src/runtime/relay/client.rs
@@ -346,6 +346,22 @@ pub(super) fn apply_client_stream_data_state(
     {
         let frontier_after = recv_stream.next_offset();
         let reorder_bytes = recv_stream.reorder_bytes();
+        lab_diagnostic(
+            "response_handoff_mux_apply",
+            format_args!(
+                "stream_id={} source_underlay={:?} source_path_index={} path_instance_id={} attachment_id={} offset={} end={} frontier_before={} frontier_after={} reorder_bytes={}",
+                stream_id.0,
+                path_key.underlay,
+                path_key.index,
+                instance.path_instance_id.as_u64(),
+                instance.attachment_id,
+                offset,
+                offset.saturating_add(payload_len as u64),
+                frontier_before,
+                frontier_after,
+                reorder_bytes,
+            ),
+        );
         if reorder_before > 0 && frontier_after > frontier_before {
             let released_bytes = outcome
                 .delivered
diff --git a/src/runtime/stream/request/attachment.rs b/src/runtime/stream/request/attachment.rs
index 7966b8d..367cf60 100644
--- a/src/runtime/stream/request/attachment.rs
+++ b/src/runtime/stream/request/attachment.rs
@@ -477,6 +477,51 @@ impl Drop for ReliableRelayInputForwarder {
     }
 }
 
+#[cfg(feature = "lab-diagnostics")]
+fn lab_response_handoff_data(frame: &Result<Frame, RuntimeError>) -> Option<(StreamId, u64, u64)> {
+    match frame {
+        Ok(Frame::StreamData {
+            stream_id,
+            offset,
+            payload,
+        }) => Some((
+            *stream_id,
+            *offset,
+            offset.saturating_add(payload.len() as u64),
+        )),
+        _ => None,
+    }
+}
+
+#[cfg(feature = "lab-diagnostics")]
+fn lab_response_handoff(
+    phase: &str,
+    instance: RelayPathInstance,
+    data: Option<(StreamId, u64, u64)>,
+    queue_observation: &str,
+    queue_items: usize,
+) {
+    let Some((stream_id, offset, end)) = data else {
+        return;
+    };
+    crate::lab_diagnostics::lab_diagnostic(
+        "response_handoff",
+        format_args!(
+            "phase={} stream_id={} path_underlay={:?} path_index={} path_instance_id={} attachment_id={} offset={} end={} queue_observation={} queue_items={}",
+            phase,
+            stream_id.0,
+            instance.key.underlay,
+            instance.key.index,
+            instance.path_instance_id.as_u64(),
+            instance.attachment_id,
+            offset,
+            end,
+            queue_observation,
+            queue_items,
+        ),
+    );
+}
+
 async fn forward_reliable_relay_attachment_frame(
     frames_tx: &mpsc::Sender<ReliableRelayRemoteFrame>,
     instance: RelayPathInstance,
@@ -488,11 +533,39 @@ async fn forward_reliable_relay_attachment_frame(
         Ok(Frame::StreamFin { .. } | Frame::StreamReset { .. })
     );
     let carrier_terminal = frame.is_err();
-    frames_tx
+    #[cfg(feature = "lab-diagnostics")]
+    let observed_data = lab_response_handoff_data(&frame);
+    #[cfg(feature = "lab-diagnostics")]
+    lab_response_handoff(
+        "shared_send_begin",
+        instance,
+        observed_data,
+        "used_slots",
+        frames_tx
+            .max_capacity()
+            .saturating_sub(frames_tx.capacity()),
+    );
+    let sent = frames_tx
         .send(ReliableRelayRemoteFrame { instance, frame })
         .await
-        .is_ok()
-        && !carrier_terminal
+        .is_ok();
+    #[cfg(feature = "lab-diagnostics")]
+    // Completion is not the channel linearization instant: another worker may
+    // log dequeue first. Used slots also include reservations, not just frames.
+    lab_response_handoff(
+        if sent {
+            "shared_send_complete"
+        } else {
+            "shared_send_failed"
+        },
+        instance,
+        observed_data,
+        "used_slots",
+        frames_tx
+            .max_capacity()
+            .saturating_sub(frames_tx.capacity()),
+    );
+    sent && !carrier_terminal
 }
 
 async fn drain_reliable_relay_attachment_after_terminal(
@@ -1256,10 +1329,22 @@ impl ReliableRelayRemoteSet {
     pub(in crate::runtime) async fn recv_frame(
         &mut self,
     ) -> Result<ReliableRelayRemoteFrame, RuntimeError> {
-        self.frames_rx
+        let received = self
+            .frames_rx
             .recv()
             .await
-            .ok_or(RuntimeError::ReliablePathSessionClosed)
+            .ok_or(RuntimeError::ReliablePathSessionClosed);
+        #[cfg(feature = "lab-diagnostics")]
+        if let Ok(frame) = &received {
+            lab_response_handoff(
+                "shared_dequeue_async",
+                frame.instance,
+                lab_response_handoff_data(&frame.frame),
+                "remaining_frames",
+                self.frames_rx.len(),
+            );
+        }
+        received
     }
 
     /// Returns the relay-input backlog visible at this instant.
@@ -1272,7 +1357,18 @@ impl ReliableRelayRemoteSet {
 
     /// Takes one already-queued frame without waiting.
     pub(in crate::runtime) fn try_recv_frame(&mut self) -> Option<ReliableRelayRemoteFrame> {
-        self.frames_rx.try_recv().ok()
+        let received = self.frames_rx.try_recv().ok();
+        #[cfg(feature = "lab-diagnostics")]
+        if let Some(frame) = &received {
+            lab_response_handoff(
+                "shared_dequeue_ready",
+                frame.instance,
+                lab_response_handoff_data(&frame.frame),
+                "remaining_frames",
+                self.frames_rx.len(),
+            );
+        }
+        received
     }
 
     #[cfg(test)]
