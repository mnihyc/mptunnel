# Current deterministic closure plan

Updated: 2026-09-10 17:36 +08:00. Authoritative repository is `./`.
**MPP is not performance-accepted. No release, push, public README update or
ideality claim. Continue the authorized closure task, not just this checkpoint.**

Read [the mandatory method](PERFORMANCE_METHOD_AND_LESSONS.md) before every
transaction and after compaction. This file is the current decision ledger,
not another issue inventory. Complete preceding chronology is preserved at
`git show 49143f2:docs-dev/CURRENT_CLOSURE_PLAN.md`; older snapshots include
`13876d6` and `ebad57f`. Reports retain raw bins, failed/adverse outcomes,
exact joins, source overlays, tests and archives. Condensation deletes no evidence.

## Decision and active transaction

**Active: failed authoritative-gap pilot; exact service-work attribution next.**

Ordinary53390 CLOSED at the unchanged85s guard:368,664,530B confirmed of
442,040,320B locally accepted; maximum confirmation gap26.666s. Not a completed
34.503Mbps result. Target writes stop entirely at423,847,786B during60–85s,
although every path remains active and native counters progress. The reverse
confirmation leg also has outstanding data. This candidate is NOT accepted.

Whole/phase evidence rejects promotion: sampled target service5–15s67.617Mbps
versus b0 321.800 BEFORE the cut; strict16→24s improves17.028→72.555 but
restored25→40s falls340.910→110.545. Cut-phase healthy-link wire service rises
31.016→165.060Mbps; extra service is real, yet not sufficient ordered delivery.
All86profiles match, no qdisc drops. Peak summed UP backlog15.06→63.22MB.
Client consumes roughly one core throughout the eventual flat interval.

Next exact question: does synchronous gap enumeration hold the request Product
lock for long scans, starving receipt/reverse delivery and prepared writers, or
are quick repeated wake/dispatch or native stages responsible? Source shows
per-boundary whole-horizon owner scans, but ordinary counters do not prove the
critical execution owner. One information-only feature overlay will aggregate
full evaluator duration, model-query and clock-query work plus evaluated region
counts using existing periodic lab perf; no per-evaluation log flood. Same
profile, ordinary failed binary retained, overlay reversed before traffic.
No runtime parameter/model change or response expansion. An inexpensive scan
would falsify that cause; a critical expensive scan selects an ownership-view
correction, not a new quantum, timer or cap. Preserve the failed ordinary
capture/archive and continuing global gates. No healthy rerun to hide failure.

The implementation/component chronology below is CLOSED, not acceptance.

Focused implementation checks are GREEN:95 distinct tests in the request actor,
flight ledger/request sender and response T06 opposite control.1.06s runtime;
last42.62s test compile. New primary test commits two disjoint repairs without
head ACK and verifies same-range suppression and expired target-slot retention.
Two migrated-fixture mistakes (pre-loss projection and queue priority order)
were corrected without changing runtime. The old request scalar clock is
removed, not retained as dead compatibility state. Shared response/io unchanged.

Ordinary release build41399 CLOSED successfully (81s), frozen at
`./.tmp/reflection/bin/authoritative-gap-service-request-20260910/mptunnel`.
Ordinary session53390 is running, tag authoritative-gap-service-qos-0910,
aggregate combined UP, same direct
200/200Mbps and46UP200→10→20015–25s,47healthy200,30/70ms, no loss/jitter/outage.
Explicitly unset per-role binaries, ROUTED, NO_QOS and all diagnostic overrides.
Existing ordinaryb0 affected/control captures are preserved; no favorable rerun.
Ordinary timing, phase-specific usefulness and wire/resource cost decide next
action. No runtime performance promotion, public update or release yet.

RED now CONFIRMED on unchanged b0: actual evaluator refuses independent second
quantum (0vs14,600B) after all real source, receipt, first-copy, successor timing,
target and capacity preconditions pass.0.21s test/42.78s compilation. Initial
private-helper compile error was fixture setup, not the RED. Implementation now
proceeds request-locally: ledger assignment clocks and evaluator service ranges
in parallel; no cargo/lab running after closed72409. Focused tests then ordinary
same-profile QoS UP decide acceptance, not this component result.

The exact capture is CLOSED. It rules out continued Original placement on the
impaired link or repairs placed back on that link during the sustained collapse.
One Q46-owned missing prefix receives successive Q47 repair quanta of14,600B
108–203ms apart, with only17–25KB accepted above the latest logged receive
frontier. Healthy47 remains200Mbps/~100ms, requiring roughly2.5MB of useful
pipeline. The ordinary cut-phase service is16.203Mbps versus185.887Mbps on the
healthy single-link control; the observer reproduces12.260Mbps, not a new
ordinary comparison. Full exact joins and limitations:
[recovery model](INDEPENDENT_QOS_RECOVERY_MODEL_20260910.md).

Root cause/model decision: existing first-missing-byte enumeration serializes
independently due authoritative omissions behind one accepted live-owner hedge.
T06 correctly prevents one14.6KB-ranked action expanding into a10.67MB suffix;
its stream-wide one-head policy does not supply sustained heterogeneous repair.
Keep each fresh rank/Apply extent bound. Select at most ONE lowest due,
serviceable authoritative missing range per existing actor evaluation, excluding
exact queued and unexpired-copy coverage as service, never as receipt. Next
evaluation follows actual commit/yield without requiring another Data ACK.
Silent retained fallback and structural failure recovery stay unchanged.

Before runtime: actual evaluator RED for a protected prefix plus independently
due disjoint successor. Preserve per-Original assignment clocks across visits
and ACK fragmentation, exact slot occupancy after suppression expiry, negative
evidence scope, native/Product admission, and future timer/capacity wakes.
Scalar first-gap timing cannot be recycled for this enumeration. The bounded
clock-owner implementation is under independent review; no new timer, quantum,
controller, reserve, hint or bottleneck grouping is authorized.

Forecast: remove the ACK-per-quantum component of the measured multi-second
repair collapse, potentially reclaiming a material fraction of the~170Mbps
healthy-link deficit. No full200Mbps floor or startup clairvoyance is promised:
actual per-range maturity, native queueing, reordered debt and physical loss
remain. New speculative copies can increase shared-congestion cost. Reject
promotion if exact successor service remains unavailable under its existing
rules, the ordinary cut-phase deficit does not materially improve, or healthy/
restored throughput, completion, gaps, latency or wire/resource costs materially
regress without a justified practical tradeoff. Do not rescue by tuning.

Rejected BEFORE implementation: post-fallback-only enumeration does not cover
the exact capture because inflated owner PTO is5.5–11.1s while copy ages are
6.8–9.1s; observed copies use the earlier authoritative-gap branch. Likewise
aggregate owner-flight ETA is not a per-range arrival proof and will not be
introduced as a new safety guarantee. Its existing advisory predicate remains.

Root owns cargo/labs; request agent owns actual evaluator RED, independent agent
reviews assignment timing/service wakes, evidence agent preserves the report.
No compiler/lab running; direct topology remains active. Target/release is
ordinary b0, observer overlay reversed and nine-file archive retained. After
focused GREEN and audit, build one ordinary candidate and run the SAME existing
aggregate QoS UP cell first, then healthy/affected ordinary controls. No public
README, release or performance acceptance at this checkpoint. Older NEXT
entries below are CLOSED chronology, not instructions to rerun them.

**QoS pair CLOSED — material heterogeneous service failure attributed.**
Both uploads settle exactly: single healthy47=177.889Mbps, aggregate46+47=
249.391Mbps whole. But aggregate RAW bins16–24 are8.196,6.573,7.147,13.352,
21.267,22.069,21.711,24.005,21.506Mbps while only46 is restricted to10Mbps;
47 remains200Mbps and the one-link control retains useful~180Mbps service.
Good whole throughput and immediate restored burst do not excuse this sustained
loss of healthy independent capacity. No random loss/jitter/UDP outage is active.
All34 files (six healthy/QoS captures plus logs/driver/shaper) are archived in
NATIVE_REFILL_INDEPENDENT_20260910.raw.tar.gz before further measurement.

Active question: is healthy47 still transmitting a suffix behind an impaired46
prefix, or does assignment/admission stop its useful native service? Reuse
ordinary per-path native counters, queues and target writes first; inspect
the existing request Original/retained/ACK-gap/copy ownership model independently.
If exact critical ownership is absent, select one existing-event diagnostic
capture, not a new tuning candidate. Forecast: this distinguishes recovery debt
from admission/placement starvation and selects a coherent root correction;
it cannot promise a gain before attribution. No timeout/reserve/hint changes,
fixed bottleneck grouping or protocol preference. Pause the additional loss/
blackhole gate until this existing failure is understood. Same direct topology
remains active; no lab/compiler is running after closed session65984.

Ordinary attribution: healthy Q47 ACKs advance38.764MB during15–17s while target
writes advance1.786MB. Later47 remains~100ms RTT but underloaded; impairedQ46
retains22–31MB Product flight. Client read minus target writes is near64MiB.
Sink confirmations normally return promptly, so this is not the reverse ACK
hold. Strict16–24s confirms46~10Mbps,47~31Mbps versus single47~196Mbps;
allqdisc drops0. Ordered service16–24 averages16.203vs185.887Mbps control.

NEXT one information-only exact request capture on SAME profile/current b0:
ordinary telemetry lacks current prepared Original DSN ownership. Existing
sender_service_decision covers repairs, not prepared Original claims. A TEMPORARY
lab-diagnostics-only event at successful commit_fenced_frame_product records
selected-stream exact DSN/path/cause and accepted-copy deadline; no decisions,
clock renewal or public metrics change. Existing server_receive_hole and
server_receive_delivery_stall supply actual receive frontiers. Include existing
relay_local_read_blocked, reinjection, request_retained_frontier_reinjection,
data_ack_loss_timer, feedback_return. Periodic perf/all-ACK tracing stays off.
One feature build; archive then reverse overlay before traffic, retain ordinary
binary separately. No new passing performance claim from observed build.

Competing causes: continued bad-path Original assignment, timely copies placed
back on46, or too little in-flight distinct repair service despite healthy47.
Exact request Original+copy intervals over the measured collapse decide among
them; finite same-range suppression must not be confused with whole-stream
service. Preserve T06 score/Apply range equality and original amplification
counterexample before any model proposal. Forecast: locate those decisions,
not a guaranteed speed gain. Nonrecurrence or observer overhead remains honest;
do not turn it into favorable ordinary evidence or retune timers/quantum.

**Independent healthy pair CLOSED:** one200→two×200 gives DOWN148.236→302.319Mbps,
echo median477→150/p95918→262ms; UP exact169.362→315.648Mbps. Both independent
class counters carry roughlyequalbulk work; no false four/eight-link sum.
All four captures complete (DOWNduration-partial, UPexact). Costsand minor
UPtiming changesremain: confirmationgap.411→.420s, localwritegap.317→.528s,
CPU/RSSgrow.169profile rowsverify200Mbps percut/30–70ms/zeroimpairments.

NEXT smallest existing heterogeneous recovery discriminator: same direct topology,
singlemixedUPon47 versusaggregateUP46+47,200Mbps each, no loss/jitter/UDPoutage.
Only link46 UP data capacity changes200→10→200Mbps at15–25s;47 stays200.
Question: does temporary poor extra capacity choke ordered service despite a
healthy independent alternative, or recover/preserve useful aggregation?
Forecast: aggregate should materially use both before/after, and retain useful
healthy-link service duringthecut, with unavoidable initialinflight/reordering
transients disclosed. No instantaneousclairvoyantfloor is asserted. A sustained
collapse below the singlehealthycontrol selects exactownership/recovery before
adding simultaneousloss/blackholes. Do not changehint/threshold/guard to pass.
Tag native-refill-independent-qos-0910, lognative-refill-independent-qos-0910.log.
The wholeUDP and randomloss additions follow only after this bounded result.

**Completed preparation for independent200Mbps links:** All12combined stress
cells are captured and61rawfiles archived before any topology/driver change.
See NATIVE_REFILL_COMBINED_20260910: currentDOWN TCP/Q/mixed/raw/Xray/H2
404.684/311.418/312.384/363.575/299.315/144.912Mbps. Loaded latency remains
adverse, soleUDP echo disconnects are explicit, and TCP/H2/XrayUP have distinct
censored/terminal-closure outcomes. This is practical evidence, not full closure.
No new runtime correction is selected from native-loss-limited drain alone.

Question/forecast: one mixed link200 versus two independent mixed links200each,
both DOWN and UP, ordinaryb0,40s,30/70ms, no randomloss/jitter/QoS/outage first.
Does useful ordered service consume both independent cuts and materially exceed
the single-cut service without failed completion/large new gaps? The physical
sum is400Mbps before overhead, not a promisedgoodput. Aggregate capacity is not
proved merely by configured paths, summed stale rates or controller count.
Inspect both actual class counters, source/routes and fulltiming/cost. A failed
owner or materially missing aggregation selects that cause before impairment
expansion; no reserve/hint/controller adjustment to force a pass.

Reuse currentownedcontainers and targetPIDs13/19/25. After verifiedquiescence,
remove ONLY exact client47/24via46.30 and server46/24via47.30 routed routes,
attach client47.10 to existing_b and server46.20 to_a. Check all4destination
routes direct, no gateway. Router staysidle. No host shaping/recreation/sudo.
OlddefaultBBR and4CPU endpoint limits remain. BothTCP+QUIC share each200cut;
two bridges are independent shaped cuts, not independentphysicalhosts.

Minimal driver-input preparation only: optional REFLECTION_LINK_RATE selects
one uniform per-link base rate at init AND scheduledrestoration; aggregate's
old300/200and60/40 defaults remain untouched when unset. Explicit rate200
therefore avoids the oldrunner overwriting requested200. Reject an aggregate
with routed topology because its twointerfaces are opposite directions, not
parallel cuts. Dry-run command routing before live use; no Product source edit.
REFLECTION_MIRROR_IMPAIRMENT=1 retains DOWN30/UP70 even for directtopology;
REFLECTION_ROUTED must be absent. Tag native-refill-independent-200-0910.
After this gate, restore exact routednetworkstate and remove only gate-installed
endpoint HTB roots before routed reuse, or explicitlyrecord directstatecontinuity.

The chronological combined classification below is CLOSED, not a rerun queue.

**Active now:** combined mixedDOWN/UP and QUIC/H2/rawUP controls are complete.
MPPmixedUP62.511 and QUP96.986Mbps settle exactly; rawUP7.293Mbps settles
in63.979s. H2UP is censored at85s,39.120/60.097MBconfirmed/accepted, and must
not be assigned a completed goodput. Current5–15s mixed/Q/raw UP means are
40.461/171.935/8.237Mbps;35–40s142.408/100.547/3.781. The physical restriction
and observed coexistence tradeoff do not support a new arbitrary model fix.

NEXT complete the already-required same-profile six-system directional matrix
before changing topology: currentQDOWN,TCPDOWN,TCPUP, then raw/Xray/H2DOWN
and XrayUP, each previously unrun in this current cohort. Same500Mbps pinned
combined asymmetric schedule, ordinary binaries, no observer/compiler. Prior
failed H2UP is retained, not repeated. Expected information is practical
availability, timing and cost versus matched baselines in everymode/direction,
not a targetMbps in a poor phase. A new criticalMPPcompletion/recovery failure
stops expansion for attribution; native-loss-limited means alone do not.
Tag native-refill-combined-matrix-0910, lognative-refill-combined-matrix-0910.log.
QDOWN311.418Mbps loses its sole-UDP echo at the outage; TCPDOWN404.684Mbps
keeps all56echoes but p95is1191ms. TCPUP hits85s guard with126339020of152436736B
confirmed. Read-only final80→85s snapshots show every-second target and native
TCP ACK progress (+9.175MB target,14.680Mbps), stable identities;26.1MB remains
unconfirmed. This is an incomplete, steadily draining observation, not a proven
deadlock or successful full settlement. Do not fix native-loss-limited drain to
meet a lab guard. Source reset/closure occurs at runner shutdown.
The planned sequence stops there automatically; continue ONLY still-unrun
raw/Xray/H2DOWN andXrayUP, no repeat/candidate/guard change. Then existing
independent200Mbps aggregation, not more healthy5% microtrials.

No new runtime fix is supported by the preceding recovery attribution; it
located post-admission delay, not a late timer/priority wake. The active next
decision is matched directional service, not more per-byte instrumentation.
The following detailed return/outage transactions are retained evidence and
their then-current stop decisions, not instructions to repeat them.

Retain **b0baca2 native TCP refill** as the WORKING candidate, with an explicit
mixed-mode cost, not release acceptance. It removes a directly proved ~1-second
TCP unsent FIFO delay and improves TCP useful service. Both mixed DOWN orders
lose about 5% late throughput; extra repairs and several resource/latency costs
are real. Restoring the old FIFO merely to erase that smaller cost is not
justified. Neither are LOWAT tuning or blanket repair suppression.

Return-cut pair completed, both return0:393.233→417.166Mbps; all78/75echoes
succeed. Median/p95 echo267/542→364/667ms worsens, worst1045→969ms improves.
Body gap.274→.625s worsens; candidate interval11.871–12.496s is BEFORE the
15–25s restriction, not a proved return-cut stall. No performance promotion.
Full phases/native/costs are under independent reporting in
NATIVE_REFILL_RECOVERY_20260910. Session66594 is closed.

**Earlier outage stop/owner:** the separate ordinary outage pair completes with a
material candidate recovery gap1.210551s at37.045635–38.256186s, versus control
.408427s at32.472965–32.881392s. Whole384.936→392.470Mbps cannot erase it;
echo p95799→906ms, all74/71echoes succeed. UDP was restored near33s. Session70999
is closed. Broader advancement stops for this existing recovery owner, not
another healthy5% optimization. First reuse exact ordinary native/queue/progress
samples over31–39s and the existing recovery event source to discriminate
missing-prefix ownership/repair admission versus post-admission native service.
Native reuse narrows the question: candidate QUIC ACK progress restarts at35s,
earlier than control36s, and both transports advance native acknowledged bytes
during the later body gap. At rows37→38 QUIC adds14.48MB and TCP41.78MB while
sampled application progress is3.76MB. Candidate DOWN queue peaks57.26MB and
TCP RTT reaches~1s; this is not a proved gate-wake deadlock or frozen QUIC.
Exact blocked DSN/Original-copy/feedback ownership is absent in ordinary data.

COMPLETED one information-only capture using the ALREADY frozen b0 feature binary
`native-refill-copy-20260910/mptunnel`, with refill opt-out absent and periodic
perf disabled. No build or source change. Same outage-only40s mixed DOWN
profile. Enable only existing events: feedback_return,receive_hole_release,
server_data_ack_recovery,server_stale_output_recovery,server_repair_carrier_accept,
server_response_recovery_wake,server_sender_dispatch. The last event supplies
actual recovery enqueue ID/range/admitted target/queue age; production Originals
bypass that dispatcher. Do not enable all ACKs or per-bulk-frame assignment logs.

Information forecast: reconstruct the diagnostic's own material body gap and
exact carrier/stream mapping, then distinguish late feedback/queued recovery
from already-admitted repair and post-admission service. Admission is not wire
transmission; event absence is not proof of no work. Preserve current body/DSN
alignment rather than assume an old HTTP offset. Prior similar observer emitted
~33MB and changed the observed gap; retain nonrecurrence/overhead honestly.
No policy, timer, native controller or smaller queue follows an aggregate alone.

Actual result: return0,397.007Mbps, all72echoes succeed; its own1.117740s gap
36.473416–37.591156s corresponds to frontier1814516761 and a64B missing QUIC
range. First TCP repair admission is150ms after the preceding frontier release,
with dispatcher queue_delay0. Its closing receive is968ms after that admission.
The target estimate was already1018ms; accepted-copy wake is576us late, not
the missing second. Admission is not native handoff; no new priority, timer or
native-controller defect has been established. Exact mapping/header join is
under independent review. Diagnostic logs total~41MB and are not ordinary
acceptance evidence.31raw files for both ordinary pairs and observer are
archived/listed in NATIVE_REFILL_RECOVERY_20260910.raw.tar.gz.

COMPLETED bounded classification: ONE ordinary current-b0 QUIC-only outage cell,
same500/500Mbps,30/70ms,40s,UDP30–33s, no loss/jitter/QoS, normal management.
Reuse unchanged older H2/raw controls from RETURN_ROUND_ORDINARY_20260910 as
context; do not rerun the entire panel or add another micro observer. Forecast:
if current QUIC resumes sustained~450Mbps as old364 did, late mixed degradation
requires coexistence/allocation context rather than a universal QUIC recovery
ceiling. If it remains degraded, investigate that native/owner recovery instead.
This cannot prove particular copies unnecessary or all queueing unavoidable.
Preserve sole-UDP outage exposure and echo timeouts; raw TCP is unexposed.
No runtime change or performance promotion. After classifying this interval,
advance existing broader recovery/aggregation gates if no concrete defect is
supported; do not turn every worse timing sample into an endless fix obligation.
The mixed latency/copy tradeoff and original5.14s UP hold remain explicit.
Log: native-refill-quic-outage-0910.log; result:
quic-combined-down-native-refill-outage-classification-0910.

Actual current QUIC result:381.015Mbps whole, body resumes34.516434s after
4.369520s sole-UDP gap, then35–40s mean448.610Mbps.61echoes succeed, one
timeout causes13unavailable-after-disconnect records. This is not an echo pass.
No persistent QUIC rate ceiling appears. Mixed keeps the echo connection alive
through that same outage, with a shorter but later1.21s gap and much higher
loaded latency. A protocol preference would lose that availability/independent
aggregation tradeoff. No additional concrete runtime defect is supported by
the current post-admission trace. This does not prove all shared queues
unavoidable or waive final mixed competitiveness.

NEXT existing broad recovery gate, no new model: ordinary current mixed DOWN,
then UP, pinned asymmetric changing-loss/jitter/return-QoS/UDP-outage profile
listed below. Reuse old matched controls only as context until final baselines.
Information forecast: check whether composed current receipt/round/refill work
still has a severe restart-dependent stall, incomplete settlement or post-QoS
collapse under combined stress. Modest Mbps changes where all baselines are
poor are not optimization targets. Preserve bins, gaps, all failed attempts,
native progress and resource cost. An incomplete/critical result selects its
exact owner before further lab expansion. Otherwise advance the predeclared
other-carrier/aggregation gates, not another micro observer for this1s interval.
No compiler/feature observer overlap. Tag native-refill-combined-0910, log
native-refill-combined-0910.log. Current native-refill ordinary binary on both
endpoints; no policy/reserve/timeout/profile tuning and no promotion.

Combined DOWN completes:312.384Mbps, first body.853s, worst gap.803s
30.399–31.202s, all66echoes succeed; echo median/p95/max370/948/2120ms.
No restart or incomplete bulk owner; duration-partial8GiB response as declared.
Single bins over500 are buffered release, not extra physical capacity. The
UDP-blackout bins30–32 are7.34/.584/.818Mbps; timely tiny progress is not full
fallback throughput. Restored35–40 mean417.551Mbps. These are stress service
observations, not a final winning comparison. UP also completes exactly:
352845824locally accepted/target confirmed bytes in45.156199s,62.511Mbps;
first confirmation.498s, maxconfirmation gap3.059s, localwrite gap2.350s.
All46 RAW bins retained (trimmed array is not a wall-clock timeline). Zero
raw bins16/17/20/22/24 are within15–25s physical UP10Mbps restriction;
do not misidentify them as pre-QoS holds using the trimmed series.

Next discriminator before calling that low UP mean a defect: ordinary current
QUIC-only, unchanged H2, raw UP controls on the IDENTICAL combined schedule.
The UP cut carries bulk here, unlike DOWN's return-only cut; soleUDP controls
also lose their only path. Forecast: distinguish common loss/restriction service
from mixed-only allocation or generalMPPQUIC cost, using matched phase/rawbins,
confirmed settlement, firstservice/confirmationgaps and native/resource context.
Do not rank terminal-ACK-censored baseline means as exact completions. One
predeclared control sequence, not repeats until good numbers. Tag
native-refill-combined-controls-0910, lognative-refill-combined-controls-0910.log.
CurrentQUIC UP settles exactly564789248B/46.587072s,96.986Mbps, maxconfirmation
gap5.160s. Pre-restriction5–15s171.935Mbps versus mixed40.461; restored35–40s
100.547 versus mixed142.408. No universal mixed or QUIC advantage follows.
H2 reaches the existing85s runner observation guard:39120373of60096512locally
accepted bytes confirmed, then runner shutdown resets the connection. Its
3.668Mbps summary is censored, NOT exact completion or an independent H2 reset.
No full raw bins remain in its incomplete-probe output. The driver stopped the
sequence there; run only the still-unrun rawUPcell with identical settings.
Do not repeat H2 or change its guard/profile to obtain a favorable comparator.

Completed outage-only CONTROL→CANDIDATE parameters, retained: same
40s mixed DOWN/echo with500/500Mbps, DOWN30/UP70ms, wholeUDP outage30–33s,
zero configured loss/jitter/QoS. This is the already declared next recovery
gate: return pair has no critical failed completion or multi-second stall,
but its adverse timings remain. No model, reserve or deadline changes.
Log `./.tmp/reflection/native-refill-outage-pair-0910.log`; results
`./.tmp/reflection/results/mixed-combined-down-native-refill-outage-{control,candidate}-0910/`.

Completed return-pair parameters and forecast, retained for the decision:

- CONTROL: frozen ordinary `ba56290`.
- CANDIDATE: frozen ordinary `b0baca2`.
- Existing mixed DOWN bulk plus serial echo, 40s.
- DOWN500Mbps/30ms; UP return500→10→500Mbps/70ms at15–25s.
- Zero configured random loss, jitter or UDP outage. Normal management only.
- Log: `./.tmp/reflection/native-refill-return-pair-0910.log`.
- Results: `./.tmp/reflection/results/mixed-combined-down-native-refill-return-{control,candidate}-0910/`.

**Question/forecast:** does reduced TCP unsent residence help feedback/fallback
under an actual restricted return, or do extra copies and changed placement
worsen service? The native refill wake does not depend on a Product ACK or
QUIC recovery and does not shrink congestion flight. It cannot remove the
10Mbps cut or guarantee a gain. Retain first/restricted/restored timing, every
echo, read gap, native progress, exact profile and wire/CPU/RSS cost.

**Stop/next:** a critical failed completion or renewed material stall selects
its exact owner before advancement. Do not retune a reserve, deadline, profile
or guard. Otherwise assess the existing separate whole-UDP30–33s outage-only
ablation, same500/500 and30/70ms, QoS/loss/jitter off, ordinary control/candidate.
This gate concerns restart-free fallback and recovery, not only average speed.
The larger global gates below remain mandatory.

## What the latest candidate actually proves

### Native handoff cause and model

[e722c46 exact wire frontier](TCP_WIRE_FRONTIER_20260910.md) observes the selected
worst echo, not a losing copy: stream0 DSN[2432,2496), path2/server instance1,
protected interval[600083336,600083448). Native initial-send frontier W−Q stays
below its end for at least1352.786ms after acceptance and crosses by1354.336ms;
first client authentication follows70ms later. This lower bound is90.34% of the
same1497.461ms echo. A late crossing alone would only be an upper bound; the
report retains the last-below syscall bracket and sampling/selection limits.

Root cause: one-frame MPP arbitration was correct while MPP owned work, but
repeated successful writes transferred bulk into a nonpreemptive native FIFO.
Prior prepared-source and one-frame corrections must remain: they addressed
assignment ownership, not how much unsent work loses future arbitration.

The candidate combines native unsent backpressure with fresh eligibility BEFORE
Original claim and outer writer-ready publication. Blocked sources stay weak
and unclaimed; one exact-carrier native wake re-enters normal arbitration.
Input, control, cancellation, lifecycle and the one partial protected transaction
remain intact. ACK/control/repair bypass only the Original preclaim check, NOT
the socket's low-water setting. Do not gate their already-owned work as a rescue.

Linux/Android policy: TCP_NOTSENT_LOWAT L=131072 native bytes, twice existing
q=65536 maximum service quantum. Linux's below-half write-space wake leaves
roughly q reserve (~1.049ms at500Mbps). This is not complete-frame reservation:
64KiB Noise DATA occupies65602wire bytes, packetization/one partial record can
overshoot, larger codec-valid records stay legal. Native flight/cwnd/pacing,
SO_SNDBUF and Product credit are unchanged. Unsupported acquisition explicitly
retains structural-only behavior; no Windows/native cross-platform success claim.

Expected benefit was removal of much of the demonstrated0.5–1.35s local unsent
residence, not all shared-network/native delay or guaranteed Mbps. Exact socket
acquisition is transactional; live poll errors retire the exact carrier without
consuming SO_ERROR. POLLRDHUP is not full closure. Existing partial-write,
source, priority and lifecycle controls remain. Actual writer permission RED
claims65536B when unavailable; GREEN81TCP +35prepared filters and5real native
adapter tests pass (filters overlap, not additive unique counts).

### Ordinary results and costs

[Full ordinary report](NATIVE_REFILL_ORDINARY_20260910.md), archived before
further decisions, preserves10cells,408bins,438DOWNechoes and4exact UP settlements.

| Control → candidate | Useful Mbps | Main timing result |
| --- | ---: | --- |
| TCP DOWN |417.249→440.164|echo p50/p95/max938/1269/1328→300/323/508ms; gap403→321ms |
| TCP UP |422.329→449.668|exact settlement; confirmation gap514→617ms worsens |
| Mixed DOWN, first order |417.539→394.076|echo p95678→410ms, gap346→370ms |
| Mixed DOWN, reversed order |405.132→397.269|echo p95460→417ms, gap397→325ms |
| Mixed UP |414.049→410.817|exact settlement; confirmation gap465→694ms worsens |

Both mixed DOWN orders have ~5% lower25–40s body service and worse late
median/p95 echo, despite improved whole tails. No third favorable repeat.
TCP native NOTSENT falls ~48–54MB→.3MB and process CPU/RSS fall, but median
native RTT grows ~217→296ms and shared backlog grows. Mixed UP return bytes
rise22.75%, peak UP backlog18.50→33.98MB, client RSS/CPU rise. Lower unsent debt
is not blanket network-latency, cost or global competitiveness improvement.

[Copy attribution](NATIVE_REFILL_COPY_ATTRIBUTION_20260910.md) reuses the audited
four-file periodic observer plus a feature-only startup admission opt-out.
Same feature build, control/candidate, fully reversed before traffic:
accepted copies188.687→294.627MB, duplicate receipt185.753→291.826MB,
Originals2.114→2.000GB, TCP Original share65.9→27.2%. Increased persistent TCP
gap/tail repair is not startup-only; no structural/requalification substitution.
All counters reconcile with explicit close/encode/write boundaries.
No winning-copy classification or unnecessary-copy proof follows.
Diagnostic echo tails improve, so this does not explain every adverse ordinary
timing result. No more observer/threshold search is selected for this smaller
cost; move to the material return/failure gate above.

Archives: ordinary49files; reverse11files; copy-attribution16files. Root created
and listed each. Commits: b0baca2 trial,9c61280 reverse decision,49143f2 attribution.
Observer executable remains frozen; ordinary source/executable restored.

## Existing unresolved owners — not fresh fix queues

1. **Mixed healthy UP5.140055s confirmation hold.** The12-cell ordinary364d417
   panel found four zero startup bins; TCP/Q maxgaps.477/.246s. Exact missing
   byte/critical stage was not captured. One unchanged target/socket observation
   and later ordinary pairs did not reproduce it. Sink receive/reply counters
   progressed, queues0, no majorfault growth in the observer. The Python sink
   retains unused lifetime history: a real observer resource issue, NOT the
   demonstrated cause of the5s hold or an MPP leak. Do not rewrite the sink or
   declare the incident fixed from later means.
2. **ba56290 receipt-before-target-park correction.** Actual DATA actor admitted
   bytes but did not materialize their valid receipt before a blocked target
   write. One forced ACK-only offer at the FIRST actual Pending preserves the
   same pinned write, MAX grant and exact retry. Immediately-ready cadence is
   unchanged. Actual RED/GREEN93server and7release controls; keep correction
   for receipt liveness, not as a proven5s cure. Ordinary mixed UP406.6→417.2Mbps
   but gap.408→.605; DOWN390.3→399.8 but worst echo806→1046ms.
   [Full receipt evidence](TARGET_WRITE_RECEIPT_ORDINARY_20260910.md).
3. **364d417 independent return-proof rounds.** Two serialized~100ms proof
   exchanges could not fit the old~175ms successor budget. Remove successor
   inheritance; each admitted round has its own fixed native interval. New
   facts/RTT/retries cannot renew that round. Restricted DOWN202→385Mbps,
   UP340→416 and less return traffic support this model. Outage gap worsened
   .777→1.098s and repeated in reversed order .648→.948; echo summaries trade off.
   No timer rescue. Exact outage observer had fallback already active1.105s
   before its smaller gap and prompt repair admission; it did not localize the
   ordinary adverse gap. Current refill test must retain this recovery boundary.
   [Round evidence](RETURN_ROUND_ORDINARY_20260910.md),
   [outage joins](RETURN_ROUND_OUTAGE_OBSERVER_20260910.md).
4. **Native/shared contention and mixed copy service.** Direct bypass echoes
   are also slower under mixed load. Three raw TCP bodies vs one produce
   similar450Mbps but median echo214vs103ms. Native controller multiplicity
   can produce real shared delay without MPP; that does not prove all queueing
   inevitable or justify a fixed bottleneck group/protocol preference.
   Prior live-hedge suppression, QUIC-only Original filtering and native-rank
   interventions did not supply a safe universally better replacement.
5. **Older exact ownership/work corrections** retain real source/RED evidence,
   not automatic speed acceptance.9ea25e2 copy debt, e476308 advisory try-lock,
   b783cd6 stale-ready preference and d999fea ACK support have adverse/mixed
   ordinary timing. Request/response retained recovery and unique ACK attribution
   preserve exact bytes/incarnations. Restart/late-STARTUP/lifecycle fixes have
   focused checks and1941mixed+64single-mode reclamation; the deployed random
   RAM/CPU incident is still not fully attributed. Reopen only on contrary
   evidence, not hypothetical inventories.
   [Dispositions](CHANGE_DISPOSITION_20260907.md),
   [reflection](PERFORMANCE_REFLECTION_20260907.md),
   [practical acceptance](REVIEW_AND_PRACTICAL_ACCEPTANCE.md),
   [prepared ownership](PREPARED_ORIGINAL_OWNERSHIP_MODEL.md).

## Current high-capacity reference, not final publication

[Matched healthy12-cell panel](RETURN_ROUND_HEALTHY_PANEL_20260910.md) uses
ordinary364d417,500/500Mbps, DOWN30/UP70ms,40s, zero configured impairments.
H2 has explicit500/500 priors; MPP remains unconfigured discovery.

| Mode | DOWN Mbps / echo median,p95 ms | UP Mbps / settlement |
| --- | --- | --- |
| TCP |418.339 /912,1276|420.440 exact |
| QUIC |426.231 /105,160|441.011 exact |
| Mixed |389.236 /267,429|370.918 exact,5.140s confirmation hold |
| Raw |446.488 /103,124|450.576 exact |
| Xray |449.000 /103,128|439.214 lower bound; early terminal-ACK closure |
| H2 |465.296 /111,114|466.290 lower bound; early terminal-ACK closure |

Do not compare censored UP baselines as completed goodput. DOWN is intentional
40s partial8GiB service, not complete objects. Buffered bins above500Mbps are
not physical rate. These older matched results are context, not current winners.

## Unchanged global closure gates

| Order | Scope | Required practical evidence |
| --- | --- | --- |
|1|Mixed allocation/startup/stalls|Exact cause/model where demonstrated; ordinary first-service, sustained bins, gaps, loaded latency and confirmed completion |
|2|Loss/jitter/QoS/blackhole recovery|Both directions and3MPP modes; same-request restart-free recovery, no severe inherited stalls |
|3|Aggregation/shared contention|Single500Mbps, independent200Mbps links, shared/asymmetric cuts; changing loss/QoS and ablations |
|4|Experience and baselines|TCP,QUIC,default; cold/warm single/concurrent and real speed.cloudflare.com; matched raw,Xray,H2, MPTCP where available |
|5|Sustainability|Restart/churn, backpressure, CPU/RSS and post-load reclamation; no hypothetical re-audit without evidence |
|6|Publication|Honest full timing/latency series, costs and completion; README/PERFORMANCE then release only after practical competitiveness |

Pinned harsh diagnostic: routed/mirrored500Mbps, UP70/20ms delay/jitter,
DOWN30/5ms; five-second UP loss[3,8,5,6,10,3,5,8]%mean6 and DOWN
[1,2,.5,3,2,.5,1,2]%; UP10Mbps15–25s, wholeUDP outage30–33s;40s offered work,
85s runner/90s probe guards. Guards classify censoring, not Product thresholds.
Do not tune this to pass. All baselines are poor there; it is stress diagnosis,
not the final performance environment or a100Mbps target. Healthy/restored,
heterogeneous and independent high-capacity service remain essential.

Universal instantaneous optimum under unannounced future network changes is
impossible. That does not waive avoidable stalls, material performance deficits
or these finite practical gates. No release on compilation/component GREEN.

## Non-negotiable model and process boundaries

- Preserve A=prepared end,C=claimed end,U=A−C,B=U+Original debt; U→O does not
  increase B. ACK cannot exceed C; FIN waits U=0. Native receipt is not Product
  receipt. Failed/partial protected writes retain exact ownership.
- Chosen exact readiness/qualification/Native fences remain; no whole-ready-set
  invalidation or metadata-refusal readiness churn. No blocking reverse Product
  lock/guard across await. Finite work and deadlock freedom are not latency proof.
- No guessed capacity, fixed protocol preference/bottleneck partition, controller
  retuning, renewable fallback or new resource cap to rescue a lab number.
- Shelved request sampler, static ranking, relative ACK codec, wrapperless actor,
  ready ACK batching, ACK/MAX pairing, logical cadence, absolute-delay reordering,
  3N1 and isolated raw-byte hysteresis deletion remain rejected/unpromoted.
  Do not revive them without new contrary evidence and an explicit forecast.
- One independently attributable mechanism at a time; all adverse phases,
  failures/censoring and costs remain. New hypotheses are not new mandatory fixes.
  ~1Mbps/10ms micro-gains are not priority over severe stalls and completion.
- An isolated rejected hypothesis stops that candidate, not the authorized task.
  Continue to the next evidence-backed action. Genuine authority blockers still
  require user direction.

## Execution and continuity

Root owns builds/labs; no compiler or other lab overlap. Use owned Docker
`mptunnel-reflection-{client,server,router}-1` only; no sudo, host shaping or
outside-repository work. Root fs has space; no /mnt/storage mirror. No cleanup
was performed in this transaction; preserve comparators/evidence.

Frozen ordinary paths under `./.tmp/reflection/bin/`:
- `native-refill-20260910/mptunnel`: b0baca2 WORKING candidate; target/release
  currently matches this ordinary executable.
- `target-write-receipt-20260910/mptunnel`: ba56290 CONTROL.
- `return-round-20260910/mptunnel`:364d417.
- `confirmed-return-20260910/mptunnel`:0cab2b5.
- `scoped-ack-20260909/mptunnel`:b2aa215.

Frozen diagnostics are NOT ordinary controls:
`native-refill-copy-20260910`,`tcp-wire-frontier-20260910`,
`return-round-outage-observer-20260910`,`confirmed-return-observer-20260910`.
Observer overlays and exact build/run inputs are in the corresponding archives.

Only the user's seven-line `docs-dev/LIVE_OWNER_FRONTIER_WORK_BOUND.md` edit
must remain untouched/unstaged. Commit exact intermediary sets; docs-dev is
ignored and needs exact force-add. PROGRESS is an ignored continuity journal,
not force-added public documentation. Do not change AGENTS.md.

Telegram latest measured milestone:2026-09-10 08:16UTC (aggregation/QoS collapse).
Next nonurgent notification not before09:17UTC; meaningful milestones/blockers
only, no component-only success. Respect soft-frequency advice. During work
keep concise commentary within60s and poll verification/labs on minute cadence.

Before compaction: record active process/session, exact next decision, current
ordinary/diagnostic identities, uncommitted ownership and adverse/open outcomes.
Do not restart a completed experiment after compaction or conclude at an
intermediary checkpoint.
