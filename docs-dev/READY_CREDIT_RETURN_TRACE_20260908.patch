diff --git a/src/runtime/path/quic/client.rs b/src/runtime/path/quic/client.rs
index eac0c46..903fe1f 100644
--- a/src/runtime/path/quic/client.rs
+++ b/src/runtime/path/quic/client.rs
@@ -1700,6 +1700,12 @@ async fn open_client_udp_stream_on_connection(
                     let _ = frames_tx.send(Ok(frame)).await;
                     Ok(())
                 },
+                #[cfg(feature = "lab-diagnostics")]
+                (
+                    true,
+                    stream_runtime.path_index,
+                    carrier.path_instance_id.as_u64(),
+                ),
             )
             .await
         };
@@ -1818,7 +1824,13 @@ async fn open_client_udp_datagram_stream(
 ) -> Result<ClientUdpDatagramStream, RuntimeError> {
     let (mut send, recv) = carrier.connection.open_bi().await?;
     send.set_traffic_class(TrafficClass::RealtimeDatagram)?;
-    let frames = spawn_quic_path_reader(recv, runtime.codec_limits, runtime.stream_frame_queue);
+    let frames = spawn_quic_path_reader(
+        recv,
+        runtime.codec_limits,
+        runtime.stream_frame_queue,
+        #[cfg(feature = "lab-diagnostics")]
+        None,
+    );
     Ok(ClientUdpDatagramStream {
         send,
         frames,
diff --git a/src/runtime/path/quic/client_stream.rs b/src/runtime/path/quic/client_stream.rs
index c3f23b2..8414199 100644
--- a/src/runtime/path/quic/client_stream.rs
+++ b/src/runtime/path/quic/client_stream.rs
@@ -33,7 +33,13 @@ pub(super) async fn run_client_udp_stream(
     mut commands: ReliablePathCommandReceivers,
     frames: mpsc::Sender<Result<Frame, RuntimeError>>,
 ) {
-    let mut carrier_frames = spawn_quic_path_reader(recv, codec_limits, reader_queue_size);
+    let mut carrier_frames = spawn_quic_path_reader(
+        recv,
+        codec_limits,
+        reader_queue_size,
+        #[cfg(feature = "lab-diagnostics")]
+        Some((path_index, path_instance_id.as_u64(), send.request_stream_id())),
+    );
     let mut pending_frames = Vec::<Frame>::new();
     let mut path_proofs = PathProofTracker::from_limits(mux_limits);
     let mut deferred_input: Option<Result<Frame, RuntimeError>> = None;
@@ -290,7 +296,55 @@ async fn handle_client_udp_stream_input(
         }) if received_stream_id == stream_id => {
             // Product retirement closes its input before ordered FIN/detach
             // work drains. Late input must not cancel that independent writer.
-            let _ = frames.send(Ok(frame)).await;
+            #[cfg(feature = "lab-diagnostics")]
+            let response = match &frame {
+                Frame::StreamData {
+                    stream_id,
+                    offset,
+                    payload,
+                } => Some((*stream_id, *offset, payload.len())),
+                _ => None,
+            };
+            #[cfg(feature = "lab-diagnostics")]
+            if let Some((stream_id, offset, len)) = response {
+                crate::lab_diagnostics::lab_diagnostic(
+                    "response_quic_handoff",
+                    format_args!(
+                        "role=client phase=route_begin mode=actor path_index={} path_instance_id={} request_stream_id={} stream_id={} offset={} end={} len={} mailbox_free={}",
+                        path_index,
+                        path_instance_id.as_u64(),
+                        send.request_stream_id(),
+                        stream_id.0,
+                        offset,
+                        offset.saturating_add(len as u64),
+                        len,
+                        frames.capacity(),
+                    ),
+                );
+            }
+            let _queued = frames.send(Ok(frame)).await.is_ok();
+            #[cfg(feature = "lab-diagnostics")]
+            if let Some((stream_id, offset, len)) = response {
+                crate::lab_diagnostics::lab_diagnostic(
+                    "response_quic_handoff",
+                    format_args!(
+                        "role=client phase=route_end mode=actor path_index={} path_instance_id={} request_stream_id={} stream_id={} offset={} end={} len={} mailbox_free={} result={}",
+                        path_index,
+                        path_instance_id.as_u64(),
+                        send.request_stream_id(),
+                        stream_id.0,
+                        offset,
+                        offset.saturating_add(len as u64),
+                        len,
+                        frames.capacity(),
+                        if _queued {
+                            "queued"
+                        } else {
+                            "recipient_closed"
+                        },
+                    ),
+                );
+            }
         }
         Frame::StreamDetach {
             stream_id: detached_stream_id,
diff --git a/src/runtime/path/quic/client_writer.rs b/src/runtime/path/quic/client_writer.rs
index eea14ed..59d0333 100644
--- a/src/runtime/path/quic/client_writer.rs
+++ b/src/runtime/path/quic/client_writer.rs
@@ -353,6 +353,8 @@ async fn flush_client_udp_frame_batch(
         // already-claimed batch must withdraw it before entering native I/O.
         commands.withdraw_writer_ready();
     }
+    #[cfg(feature = "lab-diagnostics")]
+    let request_stream_id = send.request_stream_id();
     let result = if carrier_input_open {
         flush_udp_frame_batch_with_path_proofs_interlocked(
             send,
@@ -361,7 +363,56 @@ async fn flush_client_udp_frame_batch(
             path_proofs,
             carrier_frames,
             deferred_input,
-            |frame| try_route_client_udp_stream_frame_during_write(frame, stream_id, stream_frames),
+            |frame| {
+                #[cfg(feature = "lab-diagnostics")]
+                let response = match &frame {
+                    Frame::StreamData {
+                        stream_id,
+                        offset,
+                        payload,
+                    } => Some((*stream_id, *offset, payload.len())),
+                    _ => None,
+                };
+                #[cfg(feature = "lab-diagnostics")]
+                if let Some((stream_id, offset, len)) = response {
+                    lab_diagnostic(
+                        "response_quic_handoff",
+                        format_args!(
+                            "role=client phase=route_begin mode=interlocked request_stream_id={} stream_id={} offset={} end={} len={} mailbox_free={}",
+                            request_stream_id,
+                            stream_id.0,
+                            offset,
+                            offset.saturating_add(len as u64),
+                            len,
+                            stream_frames.capacity(),
+                        ),
+                    );
+                }
+                let result =
+                    try_route_client_udp_stream_frame_during_write(frame, stream_id, stream_frames);
+                #[cfg(feature = "lab-diagnostics")]
+                if let Some((stream_id, offset, len)) = response {
+                    lab_diagnostic(
+                        "response_quic_handoff",
+                        format_args!(
+                            "role=client phase=route_result mode=interlocked request_stream_id={} stream_id={} offset={} end={} len={} mailbox_free={} result={}",
+                            request_stream_id,
+                            stream_id.0,
+                            offset,
+                            offset.saturating_add(len as u64),
+                            len,
+                            stream_frames.capacity(),
+                            match &result {
+                                Ok(CarrierInputRoute::Routed) => "queued_or_retired",
+                                Ok(CarrierInputRoute::Mailbox(_)) => "mailbox_full_pending",
+                                Ok(CarrierInputRoute::Barrier(_)) => "barrier",
+                                Err(_) => "error",
+                            },
+                        ),
+                    );
+                }
+                result
+            },
         )
         .await
     } else {
diff --git a/src/runtime/path/quic/datagram.rs b/src/runtime/path/quic/datagram.rs
index d364ec2..35e483c 100644
--- a/src/runtime/path/quic/datagram.rs
+++ b/src/runtime/path/quic/datagram.rs
@@ -58,6 +58,8 @@ pub(super) async fn handle_server_udp_datagram_stream(
         recv,
         context.codec_limits,
         udp_path_command_queue(context.mux_limits, context.codec_limits),
+        #[cfg(feature = "lab-diagnostics")]
+        None,
     );
     let mut flows = Vec::<AcceptedServerDatagramFlow>::new();
     let mut tombstones = ServerDatagramTombstoneCache::new(context.max_udp_flows_per_session);
diff --git a/src/runtime/path/quic/io.rs b/src/runtime/path/quic/io.rs
index da9e216..4d0b47f 100644
--- a/src/runtime/path/quic/io.rs
+++ b/src/runtime/path/quic/io.rs
@@ -702,14 +702,50 @@ fn compatible_udp_path_socket_addrs(
     compatible
 }
 
+#[cfg(feature = "lab-diagnostics")]
+pub(super) fn response_trace_unix_us() -> u128 {
+    std::time::SystemTime::now()
+        .duration_since(std::time::UNIX_EPOCH)
+        .unwrap_or_default()
+        .as_micros()
+}
+
+// Diagnostic wall time spent awaiting the existing mailbox send, not CPU,
+// Pending-only time, handler cost, or evidence of Native byte availability.
+#[cfg(feature = "lab-diagnostics")]
+#[derive(Default)]
+struct ResponseReaderHandoffKind {
+    count: u64,
+    send_await_us: u128,
+    max_send_await_us: u128,
+    first_start_unix_us: u128,
+    last_end_unix_us: u128,
+    max_start_unix_us: u128,
+    max_end_unix_us: u128,
+}
+
 pub(super) fn spawn_quic_path_reader(
     mut recv: UdpPathRecvStream,
     codec_limits: CodecLimits,
     queue_size: usize,
+    #[cfg(feature = "lab-diagnostics")] diagnostic_identity: Option<(usize, u64, u64)>,
 ) -> mpsc::Receiver<Result<Frame, RuntimeError>> {
     let (frames_tx, frames_rx) = mpsc::channel(queue_size);
     tokio::spawn(async move {
+        #[cfg(feature = "lab-diagnostics")]
+        let diagnostic_identity = diagnostic_identity.filter(|_| {
+            crate::lab_diagnostics::lab_diagnostic_event_enabled("response_quic_reader")
+        });
+        #[cfg(feature = "lab-diagnostics")]
+        let (mut preceding_frames, mut preceding_send_us, mut preceding_max_send_us) =
+            (0_u64, 0_u128, 0_u128);
+        #[cfg(feature = "lab-diagnostics")]
+        let mut preceding_by_kind = std::collections::BTreeMap::<&'static str, ResponseReaderHandoffKind>::new();
+        #[cfg(feature = "lab-diagnostics")]
+        let mut preceding_window_start_unix_us = response_trace_unix_us();
         loop {
+            #[cfg(feature = "lab-diagnostics")]
+            let read_started = diagnostic_identity.map(|_| (std::time::Instant::now(), response_trace_unix_us()));
             let received = tokio::select! {
                 _ = frames_tx.closed() => return,
                 received = udp_path_read_frame(&mut recv, codec_limits) => received,
@@ -728,7 +764,109 @@ pub(super) fn spawn_quic_path_reader(
                 Err(err) => Err(err),
             };
             let done = frame.is_err();
-            if frames_tx.send(frame).await.is_err() || done {
+            #[cfg(feature = "lab-diagnostics")]
+            let response = match (diagnostic_identity, &frame) {
+                (
+                    Some((path_index, path_instance_id, native_request_stream_id)),
+                    Ok(Frame::StreamData {
+                        stream_id,
+                        offset,
+                        payload,
+                    }),
+                ) => {
+                    let end = offset.saturating_add(payload.len() as u64);
+                    let (read_started, read_start_unix_us) = read_started.expect("diagnostic reader identity");
+                    let decoded_unix_us = response_trace_unix_us();
+                    crate::lab_diagnostics::lab_diagnostic(
+                        "response_quic_reader",
+                        format_args!(
+                            "phase=decoded path_namespace=runtime path_index={} path_instance_id={} native_request_stream_id={} stream_id={} offset={} end={} reader_queue_free={} preceding_frames={} preceding_send_await_us={} preceding_max_send_await_us={} preceding_window_start_unix_us={} decoded_unix_us={} read_start_unix_us={} read_await_us={}",
+                            path_index,
+                            path_instance_id,
+                            native_request_stream_id,
+                            stream_id.0,
+                            offset,
+                            end,
+                            frames_tx.capacity(),
+                            preceding_frames,
+                            preceding_send_us,
+                            preceding_max_send_us,
+                            preceding_window_start_unix_us,
+                            decoded_unix_us,
+                            read_start_unix_us,
+                            read_started.elapsed().as_micros(),
+                        ),
+                    );
+                    for (kind, work) in &preceding_by_kind {
+                        crate::lab_diagnostics::lab_diagnostic(
+                            "response_reader_predecessors",
+                            format_args!(
+                                "path_namespace=runtime path_index={} path_instance_id={} native_request_stream_id={} stream_id={} offset={} end={} frame_kind={} count={} send_await_us={} max_send_await_us={} window_start_unix_us={} window_end_unix_us={} first_start_unix_us={} last_end_unix_us={} max_start_unix_us={} max_end_unix_us={}",
+                                path_index, path_instance_id, native_request_stream_id,
+                                stream_id.0, offset, end, kind, work.count,
+                                work.send_await_us, work.max_send_await_us,
+                                preceding_window_start_unix_us, decoded_unix_us,
+                                work.first_start_unix_us, work.last_end_unix_us,
+                                work.max_start_unix_us, work.max_end_unix_us,
+                            ),
+                        );
+                    }
+                    Some((path_index, path_instance_id, native_request_stream_id, stream_id.0, *offset, end))
+                }
+                _ => None,
+            };
+            #[cfg(feature = "lab-diagnostics")]
+            let send_started = diagnostic_identity.map(|_| (
+                std::time::Instant::now(), response_trace_unix_us(),
+                frame.as_ref().map_or("ERROR", Frame::kind_name),
+            ));
+            let sent = frames_tx.send(frame).await.is_ok();
+            #[cfg(feature = "lab-diagnostics")]
+            if let Some((started, send_start_unix_us, kind)) = send_started {
+                let send_us = started.elapsed().as_micros();
+                let send_end_unix_us = response_trace_unix_us();
+                if let Some((path_index, path_instance_id, native_request_stream_id, stream_id, offset, end)) = response {
+                    crate::lab_diagnostics::lab_diagnostic(
+                        "response_quic_reader",
+                        format_args!(
+                            "phase=mailbox_complete path_namespace=runtime path_index={} path_instance_id={} native_request_stream_id={} stream_id={} offset={} end={} send_await_us={} send_start_unix_us={} send_end_unix_us={} reader_queue_free={} sent={}",
+                            path_index,
+                            path_instance_id,
+                            native_request_stream_id,
+                            stream_id,
+                            offset,
+                            end,
+                            send_us,
+                            send_start_unix_us,
+                            send_end_unix_us,
+                            frames_tx.capacity(),
+                            sent,
+                        ),
+                    );
+                    preceding_frames = 0;
+                    preceding_send_us = 0;
+                    preceding_max_send_us = 0;
+                    preceding_by_kind.clear();
+                    preceding_window_start_unix_us = send_end_unix_us;
+                } else {
+                    preceding_frames = preceding_frames.saturating_add(1);
+                    preceding_send_us = preceding_send_us.saturating_add(send_us);
+                    preceding_max_send_us = preceding_max_send_us.max(send_us);
+                    let work = preceding_by_kind.entry(kind).or_default();
+                    if work.count == 0 {
+                        work.first_start_unix_us = send_start_unix_us;
+                    }
+                    work.count = work.count.saturating_add(1);
+                    work.send_await_us = work.send_await_us.saturating_add(send_us);
+                    work.last_end_unix_us = send_end_unix_us;
+                    if work.count == 1 || send_us > work.max_send_await_us {
+                        work.max_send_await_us = send_us;
+                        work.max_start_unix_us = send_start_unix_us;
+                        work.max_end_unix_us = send_end_unix_us;
+                    }
+                }
+            }
+            if !sent || done {
                 return;
             }
         }
diff --git a/src/runtime/path/quic/repair.rs b/src/runtime/path/quic/repair.rs
index 200cdc2..d0b9562 100644
--- a/src/runtime/path/quic/repair.rs
+++ b/src/runtime/path/quic/repair.rs
@@ -21,12 +21,15 @@ pub(super) async fn run_repair_channel<F, R>(
     stream_id: StreamId,
     limits: CodecLimits,
     route: F,
+    #[cfg(feature = "lab-diagnostics")] diagnostic_identity: (bool, usize, u64),
 ) -> Result<(), RuntimeError>
 where
     F: FnMut(Frame) -> R,
     R: Future<Output = Result<(), RuntimeError>>,
 {
     send.set_repair_priority()?;
+    #[cfg(feature = "lab-diagnostics")]
+    let native_request_stream_id = send.request_stream_id();
     let write = async {
         loop {
             let Some(work) = commands.recv().await? else {
@@ -35,12 +38,40 @@ where
                 // The parent owns and cancels this future when that completes.
                 return std::future::pending::<Result<(), RuntimeError>>().await;
             };
-            udp_path_write_frame(&mut send, work.frame(), limits).await?;
+            #[cfg(feature = "lab-diagnostics")]
+            if !diagnostic_identity.0 {
+                observe_repair_response(
+                    "write_begin",
+                    diagnostic_identity,
+                    native_request_stream_id,
+                    work.frame(),
+                    "pending",
+                );
+            }
+            let result = udp_path_write_frame(&mut send, work.frame(), limits).await;
+            #[cfg(feature = "lab-diagnostics")]
+            if !diagnostic_identity.0 {
+                observe_repair_response(
+                    "write_end",
+                    diagnostic_identity,
+                    native_request_stream_id,
+                    work.frame(),
+                    if result.is_ok() { "ok" } else { "error" },
+                );
+            }
+            result?;
             // `work` releases its exact queue/writer byte charge here, or on
             // cancellation/error. Native receipt does not settle Product debt.
         }
     };
-    let read = read_repair_channel(recv, stream_id, limits, route);
+    let read = read_repair_channel(
+        recv,
+        stream_id,
+        limits,
+        route,
+        #[cfg(feature = "lab-diagnostics")]
+        Some((diagnostic_identity, native_request_stream_id)),
+    );
     tokio::try_join!(write, read).map(|_: ((), ())| ())
 }
 
@@ -49,12 +80,17 @@ pub(super) async fn read_repair_channel<F, R>(
     stream_id: StreamId,
     limits: CodecLimits,
     mut route: F,
+    #[cfg(feature = "lab-diagnostics")] diagnostic_identity: Option<((bool, usize, u64), u64)>,
 ) -> Result<(), RuntimeError>
 where
     F: FnMut(Frame) -> R,
     R: Future<Output = Result<(), RuntimeError>>,
 {
     loop {
+        #[cfg(feature = "lab-diagnostics")]
+        let read_started = diagnostic_identity
+            .filter(|(identity, _)| identity.0)
+            .map(|_| (std::time::Instant::now(), super::io::response_trace_unix_us()));
         let frame = match udp_path_read_frame(&mut recv, limits).await {
             Ok(frame) => frame,
             // Native EOF closes only this receive half. The peer's ordinary
@@ -63,6 +99,10 @@ where
             Err(error) if udp_path_input_finished(&error) => return Ok(()),
             Err(error) => return Err(error),
         };
+        #[cfg(feature = "lab-diagnostics")]
+        let read_observation = read_started.map(|(started, start)| (
+            start, started.elapsed().as_micros(), super::io::response_trace_unix_us(),
+        ));
         match &frame {
             Frame::StreamData {
                 stream_id: owner, ..
@@ -76,6 +116,91 @@ where
                 ));
             }
         }
-        route(frame).await?;
+        #[cfg(feature = "lab-diagnostics")]
+        let response = diagnostic_identity
+            .filter(|(identity, _)| identity.0)
+            .and_then(|(identity, native_id)| {
+                observe_repair_response(
+                    "decoded_route_begin",
+                    identity,
+                    native_id,
+                    &frame,
+                    "pending",
+                );
+                match &frame {
+                    Frame::StreamData {
+                        stream_id,
+                        offset,
+                        payload,
+                    } => Some((identity, native_id, *stream_id, *offset, payload.len())),
+                    _ => None,
+                }
+            });
+        #[cfg(feature = "lab-diagnostics")]
+        if let (Some((identity, native_id, stream_id, offset, len)), Some((start_unix_us, elapsed_us, decoded_unix_us))) =
+            (response, read_observation)
+        {
+            crate::lab_diagnostics::lab_diagnostic(
+                "response_quic_reader",
+                format_args!(
+                    "phase=decoded mode=repair path_namespace=runtime path_index={} path_instance_id={} native_request_stream_id={} stream_id={} offset={} end={} read_start_unix_us={} read_await_us={} decoded_unix_us={}",
+                    identity.1, identity.2, native_id, stream_id.0, offset,
+                    offset.saturating_add(len as u64), start_unix_us, elapsed_us, decoded_unix_us,
+                ),
+            );
+        }
+        let result = route(frame).await;
+        #[cfg(feature = "lab-diagnostics")]
+        if let Some((identity, native_id, stream_id, offset, len)) = response {
+            crate::lab_diagnostics::lab_diagnostic(
+                "response_quic_handoff",
+                format_args!(
+                    "role=client phase=route_end mode=repair path_index={} path_instance_id={} native_request_stream_id={} stream_id={} offset={} end={} len={} result={}",
+                    identity.1,
+                    identity.2,
+                    native_id,
+                    stream_id.0,
+                    offset,
+                    offset.saturating_add(len as u64),
+                    len,
+                    if result.is_ok() { "route_ok" } else { "error" }
+                ),
+            );
+        }
+        result?;
+    }
+}
+
+#[cfg(feature = "lab-diagnostics")]
+fn observe_repair_response(
+    phase: &str,
+    identity: (bool, usize, u64),
+    native_id: u64,
+    frame: &Frame,
+    result: &str,
+) {
+    if let Frame::StreamData {
+        stream_id,
+        offset,
+        payload,
+    } = frame
+    {
+        crate::lab_diagnostics::lab_diagnostic(
+            "response_quic_handoff",
+            format_args!(
+                "role={} phase={} mode=repair path_namespace={} path_index={} path_instance_id={} native_request_stream_id={} stream_id={} offset={} end={} len={} result={}",
+                if identity.0 { "client" } else { "server" },
+                phase,
+                if identity.0 { "runtime" } else { "wire" },
+                identity.1,
+                identity.2,
+                native_id,
+                stream_id.0,
+                offset,
+                offset.saturating_add(payload.len() as u64),
+                payload.len(),
+                result
+            ),
+        );
     }
 }
diff --git a/src/runtime/path/quic/server_stream.rs b/src/runtime/path/quic/server_stream.rs
index fdee295..8b572af 100644
--- a/src/runtime/path/quic/server_stream.rs
+++ b/src/runtime/path/quic/server_stream.rs
@@ -346,6 +346,12 @@ pub(super) async fn handle_server_udp_reliable_stream(
                     .reliable_streams
                     .route_frame(&repair_registration, stream_id, frame)
             },
+            #[cfg(feature = "lab-diagnostics")]
+            (
+                false,
+                repair_registration.path_id().0 as usize,
+                repair_registration.path_instance_id().as_u64(),
+            ),
         )
         .await
     };
@@ -401,8 +407,13 @@ async fn run_server_udp_reliable_stream_loop(
     } = stream_context;
     let carrier_frame_queue =
         udp_reliable_stream_frame_queue(context.codec_limits, context.mux_limits);
-    let mut carrier_frames =
-        spawn_quic_path_reader(recv, context.codec_limits, carrier_frame_queue);
+    let mut carrier_frames = spawn_quic_path_reader(
+        recv,
+        context.codec_limits,
+        carrier_frame_queue,
+        #[cfg(feature = "lab-diagnostics")]
+        None,
+    );
     let mut pending_frames = Vec::<Frame>::new();
     let mut deferred_input = None;
     let mut terminal_drain_deadline = None;
diff --git a/src/runtime/path/quic/server_writer.rs b/src/runtime/path/quic/server_writer.rs
index 3ac4ab3..9db054c 100644
--- a/src/runtime/path/quic/server_writer.rs
+++ b/src/runtime/path/quic/server_writer.rs
@@ -477,6 +477,32 @@ async fn flush_server_udp_frame_batch(
     if !pending_frames.is_empty() {
         commands.withdraw_writer_ready();
     }
+    #[cfg(feature = "lab-diagnostics")]
+    let response_ranges: Vec<_> = pending_frames
+        .iter()
+        .filter(|_| crate::lab_diagnostics::lab_diagnostic_event_enabled("response_quic_handoff"))
+        .filter_map(|frame| match frame {
+            Frame::StreamData {
+                stream_id,
+                offset,
+                payload,
+            } => Some((*stream_id, *offset, payload.len())),
+            _ => None,
+        })
+        .collect();
+    #[cfg(feature = "lab-diagnostics")]
+    let native_request_stream_id = send.request_stream_id();
+    #[cfg(feature = "lab-diagnostics")]
+    for range in &response_ranges {
+        observe_server_quic_response_write(
+            "write_begin",
+            "interlocked",
+            path_registration,
+            native_request_stream_id,
+            *range,
+            "pending",
+        );
+    }
     let result = flush_udp_frame_batch_with_path_proofs_interlocked(
         send,
         pending_frames,
@@ -494,6 +520,17 @@ async fn flush_server_udp_frame_batch(
         },
     )
     .await;
+    #[cfg(feature = "lab-diagnostics")]
+    for range in &response_ranges {
+        observe_server_quic_response_write(
+            "write_end",
+            "interlocked",
+            path_registration,
+            native_request_stream_id,
+            *range,
+            if result.is_ok() { "ok" } else { "error" },
+        );
+    }
     commands.release_pending_command_bytes(std::mem::take(pending_frame_command_bytes));
     let _routed_frames = result?;
     #[cfg(feature = "lab-diagnostics")]
@@ -512,6 +549,34 @@ async fn flush_server_udp_frame_batch(
     Ok(())
 }
 
+#[cfg(feature = "lab-diagnostics")]
+fn observe_server_quic_response_write(
+    phase: &str,
+    mode: &str,
+    registration: &ServerCarrierPathRegistration,
+    native_request_stream_id: u64,
+    (stream_id, offset, len): (StreamId, u64, usize),
+    result: &str,
+) {
+    lab_diagnostic(
+        "response_quic_handoff",
+        format_args!(
+            "role=server phase={} mode={} session_id={} path_id={} path_instance_id={} native_request_stream_id={} stream_id={} offset={} end={} len={} result={}",
+            phase,
+            mode,
+            registration.session_id().0,
+            registration.path_id().0,
+            registration.path_instance_id().as_u64(),
+            native_request_stream_id,
+            stream_id.0,
+            offset,
+            offset.saturating_add(len as u64),
+            len,
+            result,
+        ),
+    );
+}
+
 fn try_route_server_udp_stream_frame_during_write(
     frame: Frame,
     stream_id: StreamId,
diff --git a/src/runtime/path/quic/tests_client.rs b/src/runtime/path/quic/tests_client.rs
index 7548bcc..73e91f2 100644
--- a/src/runtime/path/quic/tests_client.rs
+++ b/src/runtime/path/quic/tests_client.rs
@@ -822,9 +822,14 @@ async fn repair_graceful_response_eof_preserves_request_half_and_ordinary_termin
         // Consume EOF exactly once through the production read-half owner.
         // Completion of this half must not consume the independent send half
         // or cancel the parent's ordinary terminal receive/drain transaction.
-        super::super::repair::read_repair_channel(repair_recv, stream_id, limits, |_| async {
-            panic!("finished response half cannot deliver another repair frame")
-        })
+        super::super::repair::read_repair_channel(
+            repair_recv,
+            stream_id,
+            limits,
+            |_| async { panic!("finished response half cannot deliver another repair frame") },
+            #[cfg(feature = "lab-diagnostics")]
+            None,
+        )
         .await
         .expect("graceful EOF completes only the repair receive half");
         let tail = Frame::StreamData {
diff --git a/src/runtime/path/tcp/client/connection.rs b/src/runtime/path/tcp/client/connection.rs
index 1686b83..df3ee99 100644
--- a/src/runtime/path/tcp/client/connection.rs
+++ b/src/runtime/path/tcp/client/connection.rs
@@ -226,6 +226,27 @@ pub(in crate::runtime) async fn connect_client_tcp_carrier(
             reader,
             reliable_path_writer_frame_queue(mux_limits),
             move |frame| {
+                #[cfg(feature = "lab-diagnostics")]
+                if let Frame::StreamData {
+                    stream_id,
+                    offset,
+                    payload,
+                } = frame
+                {
+                    crate::lab_diagnostics::lab_diagnostic(
+                        "response_tcp_handoff",
+                        format_args!(
+                            "role=client phase=authenticated_decode session_id={} path_namespace=wire path_id={} remote_port={} stream_id={} offset={} end={} len={}",
+                            session_id.0,
+                            path_id.0,
+                            remote_port,
+                            stream_id.0,
+                            offset,
+                            offset.saturating_add(payload.len() as u64),
+                            payload.len(),
+                        ),
+                    );
+                }
                 if let Frame::SessionClose { reason } = frame {
                     observed_lifecycle.retire(*reason);
                 }
diff --git a/src/runtime/path/tcp/client/stream.rs b/src/runtime/path/tcp/client/stream.rs
index 8118f43..6f2cdef 100644
--- a/src/runtime/path/tcp/client/stream.rs
+++ b/src/runtime/path/tcp/client/stream.rs
@@ -477,7 +477,23 @@ pub(in crate::runtime::path::tcp) async fn handle_client_tcp_stream_frame(
             offset,
             payload,
         } => {
-            route_client_tcp_stream_frame(
+            #[cfg(feature = "lab-diagnostics")]
+            let response_len = payload.len();
+            #[cfg(feature = "lab-diagnostics")]
+            lab_diagnostic(
+                "response_tcp_handoff",
+                format_args!(
+                    "role=client phase=route_begin mode=actor path_index={} path_instance_id={} stream_id={} offset={} end={} len={} mailbox_free={:?}",
+                    runtime.path_index,
+                    connection.path_instance_id.as_u64(),
+                    stream_id.0,
+                    offset,
+                    offset.saturating_add(response_len as u64),
+                    response_len,
+                    streams.get(&stream_id).map(|state| state.frames.capacity()),
+                ),
+            );
+            let result = route_client_tcp_stream_frame(
                 streams,
                 closed_streams,
                 stream_id,
@@ -487,7 +503,23 @@ pub(in crate::runtime::path::tcp) async fn handle_client_tcp_stream_frame(
                     payload,
                 },
             )
-            .await
+            .await;
+            #[cfg(feature = "lab-diagnostics")]
+            lab_diagnostic(
+                "response_tcp_handoff",
+                format_args!(
+                    "role=client phase=route_end mode=actor path_index={} path_instance_id={} stream_id={} offset={} end={} len={} attachment_present={} result={}",
+                    runtime.path_index,
+                    connection.path_instance_id.as_u64(),
+                    stream_id.0,
+                    offset,
+                    offset.saturating_add(response_len as u64),
+                    response_len,
+                    streams.contains_key(&stream_id),
+                    if result.is_ok() { "ok" } else { "error" },
+                ),
+            );
+            result
         }
         Frame::StreamAck {
             stream_id,
diff --git a/src/runtime/path/tcp/client/writer.rs b/src/runtime/path/tcp/client/writer.rs
index c3bacfe..14e875b 100644
--- a/src/runtime/path/tcp/client/writer.rs
+++ b/src/runtime/path/tcp/client/writer.rs
@@ -877,7 +877,54 @@ fn try_route_client_tcp_frame_during_write_for_session(
             Err(error) => Err(error),
         };
     }
-    try_route_client_tcp_frame_during_write(frame, streams, closed_streams, datagrams)
+    #[cfg(feature = "lab-diagnostics")]
+    let response = match &frame {
+        Frame::StreamData {
+            stream_id,
+            offset,
+            payload,
+        } => {
+            lab_diagnostic(
+                "response_tcp_handoff",
+                format_args!(
+                    "role=client phase=route_begin mode=interlocked path_index={} path_instance_id={} stream_id={} offset={} end={} len={} mailbox_free={:?}",
+                    runtime.path_index,
+                    path_instance_id.as_u64(),
+                    stream_id.0,
+                    offset,
+                    offset.saturating_add(payload.len() as u64),
+                    payload.len(),
+                    streams.get(stream_id).map(|state| state.frames.capacity()),
+                ),
+            );
+            Some((*stream_id, *offset, payload.len()))
+        }
+        _ => None,
+    };
+    let result = try_route_client_tcp_frame_during_write(frame, streams, closed_streams, datagrams);
+    #[cfg(feature = "lab-diagnostics")]
+    if let Some((stream_id, offset, len)) = response {
+        lab_diagnostic(
+            "response_tcp_handoff",
+            format_args!(
+                "role=client phase=route_result mode=interlocked path_index={} path_instance_id={} stream_id={} offset={} end={} len={} attachment_present={} result={}",
+                runtime.path_index,
+                path_instance_id.as_u64(),
+                stream_id.0,
+                offset,
+                offset.saturating_add(len as u64),
+                len,
+                streams.contains_key(&stream_id),
+                match &result {
+                    Ok(ClientTcpWriteFrameRoute::Routed) => "routed_or_retired",
+                    Ok(ClientTcpWriteFrameRoute::Mailbox { .. }) => "mailbox_full_pending",
+                    Ok(ClientTcpWriteFrameRoute::Barrier(_)) => "barrier",
+                    Err(_) => "error",
+                },
+            ),
+        );
+    }
+    result
 }
 
 fn try_route_client_tcp_frame_during_write(
diff --git a/src/runtime/path/tcp/server/session.rs b/src/runtime/path/tcp/server/session.rs
index b0a597a..f09937f 100644
--- a/src/runtime/path/tcp/server/session.rs
+++ b/src/runtime/path/tcp/server/session.rs
@@ -1018,7 +1018,18 @@ impl ServerTcpPathSession {
                 .commit_transaction_interlocked(writer_pending_bytes)
                 .await;
         }
-        let wrote = self.writer.commit_transaction(&mut self.evidence).await?;
+        let wrote = self
+            .writer
+            .commit_transaction(
+                &mut self.evidence,
+                #[cfg(feature = "lab-diagnostics")]
+                (
+                    self.path_registration.session_id().0,
+                    self.path_id.0 as usize,
+                    self.path_registration.path_instance_id().as_u64(),
+                ),
+            )
+            .await?;
         if wrote && *writer_pending_bytes > 0 {
             self.evidence
                 .observe_after_write(&self.context, &self.path_registration, self.path_id);
@@ -1042,7 +1053,15 @@ impl ServerTcpPathSession {
         let mut routed_frames = 0usize;
         let mut mailbox: Option<PendingMailboxFrame> = None;
         let write_result = {
-            let write = self.writer.commit_transaction(&mut self.evidence);
+            let write = self.writer.commit_transaction(
+                &mut self.evidence,
+                #[cfg(feature = "lab-diagnostics")]
+                (
+                    self.path_registration.session_id().0,
+                    self.path_id.0 as usize,
+                    self.path_registration.path_instance_id().as_u64(),
+                ),
+            );
             tokio::pin!(write);
             loop {
                 tokio::select! {
diff --git a/src/runtime/path/tcp/server/writer.rs b/src/runtime/path/tcp/server/writer.rs
index 0ccb601..7798e3b 100644
--- a/src/runtime/path/tcp/server/writer.rs
+++ b/src/runtime/path/tcp/server/writer.rs
@@ -53,15 +53,24 @@ impl ServerTcpWriter {
     pub(in crate::runtime::path::tcp) async fn commit_transaction(
         &mut self,
         evidence: &mut ServerTcpEvidenceState,
+        #[cfg(feature = "lab-diagnostics")] diagnostic_identity: (u64, usize, u64),
     ) -> Result<bool, RuntimeError> {
         if self.pending_frames.is_empty() {
             return Ok(true);
         }
+        #[cfg(feature = "lab-diagnostics")]
+        self.observe_response_transaction("write_begin", diagnostic_identity, "pending");
         let commit = async {
             self.framed.write_frames(&self.pending_frames).await?;
             self.framed.flush().await
         }
         .await;
+        #[cfg(feature = "lab-diagnostics")]
+        self.observe_response_transaction(
+            "flush_end",
+            diagnostic_identity,
+            if commit.is_ok() { "ok" } else { "error" },
+        );
         match commit {
             Ok(()) => {
                 for frame in &self.pending_frames {
@@ -75,6 +84,39 @@ impl ServerTcpWriter {
         }
     }
 
+    #[cfg(feature = "lab-diagnostics")]
+    fn observe_response_transaction(
+        &self,
+        phase: &str,
+        (session_id, path_index, path_instance_id): (u64, usize, u64),
+        result: &str,
+    ) {
+        for frame in &self.pending_frames {
+            if let Frame::StreamData {
+                stream_id,
+                offset,
+                payload,
+            } = frame
+            {
+                crate::lab_diagnostics::lab_diagnostic(
+                    "response_tcp_handoff",
+                    format_args!(
+                        "role=server phase={} session_id={} path_namespace=wire path_id={} path_instance_id={} stream_id={} offset={} end={} len={} result={}",
+                        phase,
+                        session_id,
+                        path_index,
+                        path_instance_id,
+                        stream_id.0,
+                        offset,
+                        offset.saturating_add(payload.len() as u64),
+                        payload.len(),
+                        result,
+                    ),
+                );
+            }
+        }
+    }
+
     pub(in crate::runtime::path::tcp) async fn write_frame(
         &mut self,
         frame: &Frame,
diff --git a/src/runtime/relay/client.rs b/src/runtime/relay/client.rs
index 23783ec..16d9df2 100644
--- a/src/runtime/relay/client.rs
+++ b/src/runtime/relay/client.rs
@@ -344,6 +344,22 @@ pub(super) fn apply_client_stream_data_state(
     {
         let frontier_after = recv_stream.next_offset();
         let reorder_bytes = recv_stream.reorder_bytes();
+        lab_diagnostic(
+            "response_handoff_mux_apply",
+            format_args!(
+                "stream_id={} source_underlay={:?} source_path_index={} path_instance_id={} attachment_id={} offset={} end={} frontier_before={} frontier_after={} reorder_bytes={}",
+                stream_id.0,
+                path_key.underlay,
+                path_key.index,
+                instance.path_instance_id.as_u64(),
+                instance.attachment_id,
+                offset,
+                offset.saturating_add(payload_len as u64),
+                frontier_before,
+                frontier_after,
+                reorder_bytes,
+            ),
+        );
         if reorder_before > 0 && frontier_after > frontier_before {
             let released_bytes = outcome
                 .delivered
diff --git a/src/runtime/relay/control.rs b/src/runtime/relay/control.rs
index 6a1a850..0e18eaf 100644
--- a/src/runtime/relay/control.rs
+++ b/src/runtime/relay/control.rs
@@ -3715,6 +3715,15 @@ where
                                         }
                                     }
                                 };
+                                #[cfg(feature = "lab-diagnostics")]
+                                if let Ok(bytes) = &write {
+                                    if *bytes > 0 {
+                                        lab_diagnostic("terminal_service", format_args!(
+                                            "stage=reply_delivered role=client stream_id={} bytes={} receive_frontier={} request_open={} fin_sent={}",
+                                            stream_id.0, bytes, recv_stream.next_offset(), state.endpoint.local_open, state.endpoint.local_fin_sent,
+                                        ));
+                                    }
+                                }
                                 if let Err(err) = write {
                                     break Err(err);
                                 }
diff --git a/src/runtime/relay/server.rs b/src/runtime/relay/server.rs
index 60d4436..bc6da89 100644
--- a/src/runtime/relay/server.rs
+++ b/src/runtime/relay/server.rs
@@ -4046,6 +4046,15 @@ where
             let result =
                 read_reliable_relay_payload(&mut local, &mut buf, reserved_read_budget).await;
             #[cfg(feature = "lab-diagnostics")]
+            if let Ok((read, _)) = &result {
+                if *read > 0 {
+                    lab_diagnostic("terminal_service", format_args!(
+                        "stage=target_reply_read role=server session_id={} stream_id={} bytes={} read_await_us={} opportunistic=false",
+                        session_id.0, stream_id.0, read, read_started.elapsed().as_micros(),
+                    ));
+                }
+            }
+            #[cfg(feature = "lab-diagnostics")]
             if let Ok((read, _)) = &result {
                 lab_perf_record("relay.local_read_wait", read_started.elapsed(), *read);
             }
@@ -4062,8 +4071,19 @@ where
             } else {
                 {
                     let mut product = response_product.lock();
+                    #[cfg(feature = "lab-diagnostics")]
+                    let (claimed_end, source_before) =
+                        (product.send_stream.next_offset(), product.sender.data_bytes());
                     let payload = payload.expect("positive read returns payload");
                     let enqueue_id = product.sender.enqueue_data_for_lane(payload, response_lane);
+                    #[cfg(feature = "lab-diagnostics")]
+                    lab_diagnostic("terminal_service", format_args!(
+                        "stage=response_source_enqueue role=server session_id={} stream_id={} enqueue_id={} bytes={} claimed_end={} source_before={} source_after={} offset={} end={} opportunistic=false",
+                        session_id.0, stream_id.0, enqueue_id, read, claimed_end,
+                        source_before, product.sender.data_bytes(),
+                        claimed_end.saturating_add(source_before as u64),
+                        claimed_end.saturating_add(source_before as u64).saturating_add(read as u64),
+                    ));
                     #[cfg(not(feature = "lab-diagnostics"))]
                     let _ = enqueue_id;
                     #[cfg(feature = "lab-diagnostics")]
@@ -4114,9 +4134,20 @@ where
                             let permit = session_send_buffer
                                 .reserve(&mut send_buffer_updates, next_read_budget)
                                 .await;
+                            #[cfg(feature = "lab-diagnostics")]
+                            let read_started = Instant::now();
                             let result = read_reliable_relay_payload(
                                 &mut local, &mut buf, permit.bytes(),
                             ).await;
+                            #[cfg(feature = "lab-diagnostics")]
+                            if let Ok((read, _)) = &result {
+                                if *read > 0 {
+                                    lab_diagnostic("terminal_service", format_args!(
+                                        "stage=target_reply_read role=server session_id={} stream_id={} bytes={} read_await_us={} opportunistic=true",
+                                        session_id.0, stream_id.0, read, read_started.elapsed().as_micros(),
+                                    ));
+                                }
+                            }
                             (result, permit)
                         } => read,
                         _ = std::future::ready(()) => break,
@@ -4132,8 +4163,19 @@ where
                     }
                     {
                         let mut product = response_product.lock();
+                        #[cfg(feature = "lab-diagnostics")]
+                        let (claimed_end, source_before) =
+                            (product.send_stream.next_offset(), product.sender.data_bytes());
                         let payload = payload.expect("positive read returns payload");
                         let enqueue_id = product.sender.enqueue_data_for_lane(payload, response_lane);
+                        #[cfg(feature = "lab-diagnostics")]
+                        lab_diagnostic("terminal_service", format_args!(
+                            "stage=response_source_enqueue role=server session_id={} stream_id={} enqueue_id={} bytes={} claimed_end={} source_before={} source_after={} offset={} end={} opportunistic=true",
+                            session_id.0, stream_id.0, enqueue_id, read, claimed_end,
+                            source_before, product.sender.data_bytes(),
+                            claimed_end.saturating_add(source_before as u64),
+                            claimed_end.saturating_add(source_before as u64).saturating_add(read as u64),
+                        ));
                         #[cfg(not(feature = "lab-diagnostics"))]
                         let _ = enqueue_id;
                         #[cfg(feature = "lab-diagnostics")]
diff --git a/src/runtime/sender/response/prepared.rs b/src/runtime/sender/response/prepared.rs
index 4bddf94..34eba71 100644
--- a/src/runtime/sender/response/prepared.rs
+++ b/src/runtime/sender/response/prepared.rs
@@ -458,6 +458,23 @@ pub(in crate::runtime) fn claim_prepared_response_data(
                         *offset,
                         payload.len(),
                     );
+                    crate::lab_diagnostics::lab_diagnostic(
+                        "response_original_claim",
+                        format_args!(
+                            "session_id={} stream_id={} kind=Original path_namespace=wire underlay={:?} path_id={} path_instance_id={} attachment={} offset={} end={} len={} claimed_end={} source_bytes={}",
+                            state.sender.session_id.0,
+                            state.sender.stream_id.0,
+                            identity.key.underlay,
+                            identity.key.path_id.0,
+                            identity.path_instance_id.as_u64(),
+                            identity.incarnation,
+                            offset,
+                            offset.saturating_add(payload.len() as u64),
+                            payload.len(),
+                            state.send_stream.next_offset(),
+                            state.sender.data_bytes(),
+                        ),
+                    );
                 }
                 Some(PreparedOriginalClaim::Claimed(frame.clone()))
             }
diff --git a/src/runtime/stream/request/attachment.rs b/src/runtime/stream/request/attachment.rs
index 2c5faeb..46e8b54 100644
--- a/src/runtime/stream/request/attachment.rs
+++ b/src/runtime/stream/request/attachment.rs
@@ -478,6 +478,51 @@ impl Drop for ReliableRelayInputForwarder {
     }
 }
 
+#[cfg(feature = "lab-diagnostics")]
+fn lab_response_handoff_data(frame: &Result<Frame, RuntimeError>) -> Option<(StreamId, u64, u64)> {
+    match frame {
+        Ok(Frame::StreamData {
+            stream_id,
+            offset,
+            payload,
+        }) => Some((
+            *stream_id,
+            *offset,
+            offset.saturating_add(payload.len() as u64),
+        )),
+        _ => None,
+    }
+}
+
+#[cfg(feature = "lab-diagnostics")]
+fn lab_response_handoff(
+    phase: &str,
+    instance: RelayPathInstance,
+    data: Option<(StreamId, u64, u64)>,
+    queue_observation: &str,
+    queue_items: usize,
+) {
+    let Some((stream_id, offset, end)) = data else {
+        return;
+    };
+    crate::lab_diagnostics::lab_diagnostic(
+        "response_handoff",
+        format_args!(
+            "phase={} stream_id={} path_underlay={:?} path_index={} path_instance_id={} attachment_id={} offset={} end={} queue_observation={} queue_items={}",
+            phase,
+            stream_id.0,
+            instance.key.underlay,
+            instance.key.index,
+            instance.path_instance_id.as_u64(),
+            instance.attachment_id,
+            offset,
+            end,
+            queue_observation,
+            queue_items,
+        ),
+    );
+}
+
 async fn forward_reliable_relay_attachment_frame(
     frames_tx: &mpsc::Sender<ReliableRelayRemoteFrame>,
     instance: RelayPathInstance,
@@ -489,11 +534,39 @@ async fn forward_reliable_relay_attachment_frame(
         Ok(Frame::StreamFin { .. } | Frame::StreamReset { .. })
     );
     let carrier_terminal = frame.is_err();
-    frames_tx
+    #[cfg(feature = "lab-diagnostics")]
+    let observed_data = lab_response_handoff_data(&frame);
+    #[cfg(feature = "lab-diagnostics")]
+    lab_response_handoff(
+        "shared_send_begin",
+        instance,
+        observed_data,
+        "used_slots",
+        frames_tx
+            .max_capacity()
+            .saturating_sub(frames_tx.capacity()),
+    );
+    let sent = frames_tx
         .send(ReliableRelayRemoteFrame { instance, frame })
         .await
-        .is_ok()
-        && !carrier_terminal
+        .is_ok();
+    #[cfg(feature = "lab-diagnostics")]
+    // Completion is not the channel linearization instant: another worker may
+    // log dequeue first. Used slots also include reservations, not just frames.
+    lab_response_handoff(
+        if sent {
+            "shared_send_complete"
+        } else {
+            "shared_send_failed"
+        },
+        instance,
+        observed_data,
+        "used_slots",
+        frames_tx
+            .max_capacity()
+            .saturating_sub(frames_tx.capacity()),
+    );
+    sent && !carrier_terminal
 }
 
 async fn drain_reliable_relay_attachment_after_terminal(
@@ -638,7 +711,16 @@ impl ReliableRelayRemoteInput {
                 .await
                 .ok_or(RuntimeError::ReliablePathSessionClosed)?,
         };
-        Ok(self.fold_ready_max_data(first))
+        let received = self.fold_ready_max_data(first);
+        #[cfg(feature = "lab-diagnostics")]
+        lab_response_handoff(
+            "shared_dequeue_async",
+            received.instance,
+            lab_response_handoff_data(&received.frame),
+            "actor_ready_frames",
+            self.ready_frame_count(),
+        );
+        Ok(received)
     }
 
     /// MAX_DATA is a shared monotonic grant, not byte receipt or path proof.
@@ -674,6 +756,16 @@ impl ReliableRelayRemoteInput {
                 }
                 _ => {
                     debug_assert!(self.pending_frame.is_none());
+                    #[cfg(feature = "lab-diagnostics")]
+                    // A prefetched Data boundary has left the MPSC queue but
+                    // has not yet been returned to the actor by either API.
+                    lab_response_handoff(
+                        "shared_prefetch_boundary",
+                        next.instance,
+                        lab_response_handoff_data(&next.frame),
+                        "buffered_mpsc_frames",
+                        self.frames_rx.len(),
+                    );
                     self.pending_frame = Some(next);
                     break;
                 }
@@ -698,7 +790,16 @@ impl ReliableRelayRemoteInput {
             .pending_frame
             .take()
             .or_else(|| self.frames_rx.try_recv().ok())?;
-        Some(self.fold_ready_max_data(first))
+        let received = self.fold_ready_max_data(first);
+        #[cfg(feature = "lab-diagnostics")]
+        lab_response_handoff(
+            "shared_dequeue_ready",
+            received.instance,
+            lab_response_handoff_data(&received.frame),
+            "actor_ready_frames",
+            self.ready_frame_count(),
+        );
+        Some(received)
     }
 
     #[cfg(test)]
