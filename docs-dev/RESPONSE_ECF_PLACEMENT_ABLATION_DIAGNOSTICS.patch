diff --git a/src/runtime/sender/response/scheduling.rs b/src/runtime/sender/response/scheduling.rs
index 042d41b..ef55afd 100644
--- a/src/runtime/sender/response/scheduling.rs
+++ b/src/runtime/sender/response/scheduling.rs
@@ -7,8 +7,10 @@
 #[cfg(feature = "lab-diagnostics")]
 use crate::lab_diagnostics::lab_diagnostic;
 use crate::model::admission::{
-    BulkCandidatePosition, BulkProductResourceCheck, ReliableDataAckFrontierState,
-    bulk_contiguous_frontier_can_accept_enqueue, bulk_product_candidate_resource_suppression,
+    BulkAdmissionCheck, BulkAdmissionEvidence, BulkCandidatePosition, BulkProductResourceCheck,
+    ReliableDataAckFrontierState, bulk_contiguous_frontier_can_accept_enqueue,
+    bulk_measurement_start_suppression_with_completion_backlog,
+    bulk_product_candidate_resource_suppression,
     original_data_assignment_has_product_headroom,
 };
 use crate::model::capacity::reliable_unproven_path_startup_flight_limit_bytes;
@@ -249,11 +251,61 @@ pub(super) fn select_response_data_path_with_payload(
                     may_attempt_product_admission
                         .then(|| {
                             bulk_product_candidate_resource_suppression(admission_check)
+                                .or_else(|| {
+                                    // Diagnostic ablation only: restore the former
+                                    // response placement comparison without its
+                                    // inference-derived resource limit.
+                                    let (best_snapshot, best_eta_ms) = if owns_lower_frontier {
+                                        (snapshot, eta_ms)
+                                    } else {
+                                        (lead.1, lead.2)
+                                    };
+                                    bulk_measurement_start_suppression_with_completion_backlog(
+                                        BulkAdmissionCheck {
+                                            best_snapshot,
+                                            best_eta_ms,
+                                            candidate_snapshot: snapshot,
+                                            candidate_eta_ms: eta_ms,
+                                            payload_bytes: target_payload_bytes,
+                                            mux_limits,
+                                            position,
+                                            stream_ordering_debt_bytes: external_flight,
+                                        },
+                                        lead.0.observation.original_data_in_flight_bytes,
+                                        BulkAdmissionEvidence {
+                                            product_assignment_qualified: target
+                                                .observation
+                                                .product_assignment_qualified,
+                                            fresh_completion_rate: target
+                                                .observation
+                                                .has_bulk_rate_evidence,
+                                        },
+                                    )
+                                })
                         })
                     .flatten()
                     .or((!may_attempt_product_admission).then_some("startup_flight_limit"))
                 };
                 #[cfg(feature = "lab-diagnostics")]
+                if suppression == Some("ecf_no_completion_gain") {
+                    static FIRST_ECF_REJECTION: std::sync::atomic::AtomicBool =
+                        std::sync::atomic::AtomicBool::new(false);
+                    if !FIRST_ECF_REJECTION.swap(true, std::sync::atomic::Ordering::Relaxed) {
+                        lab_diagnostic(
+                            "response_ecf_ablation_first_rejection",
+                            format_args!(
+                                "path_underlay={:?} path_id={} position={:?} eta_ms={:.3} lead_underlay={:?} lead_path_id={} lead_eta_ms={:.3} lead_original_flight={} candidate_original_flight={} candidate_fresh_rate={} payload_bytes={}",
+                                key.underlay, key.path_id.0, position, eta_ms,
+                                lead_key.underlay, lead_key.path_id.0, lead.2,
+                                lead.0.observation.original_data_in_flight_bytes,
+                                target.observation.original_data_in_flight_bytes,
+                                target.observation.has_bulk_rate_evidence,
+                                target_payload_bytes,
+                            ),
+                        );
+                    }
+                }
+                #[cfg(feature = "lab-diagnostics")]
                 lab_diagnostic(
                     "response_data_candidate_evaluated",
                     format_args!(
