{
  "status": "Temporary diagnostic-only overlay; not a model change; remove all active hooks after attribution",
  "flag": "MPTUNNEL_NATIVE_MODEL_TRACE=1",
  "cadence": "State/deadline at most once per second on existing calls; one completed-budget record per native round; every bound action; late proof only when maximum or persistence counter changes",
  "bbr3_diff": "diff --git a/crates/quinn-proto/src/congestion/bbr3/mod.rs b/crates/quinn-proto/src/congestion/bbr3/mod.rs\nindex d6de9b3..0a0c4d7 100644\n--- a/crates/quinn-proto/src/congestion/bbr3/mod.rs\n+++ b/crates/quinn-proto/src/congestion/bbr3/mod.rs\n@@ -1358,6 +1358,17 @@ impl Bbr3 {\n         };\n         let (next, round_high) = self.advance_loss_budget_state(current, &epoch)?;\n         let completed_events = self.loss_budget_epoch_events(&epoch);\n+        if std::env::var_os(\"MPTUNNEL_NATIVE_MODEL_TRACE\").is_some_and(|v| v == \"1\") {\n+            let unix_ms = std::time::SystemTime::now()\n+                .duration_since(std::time::UNIX_EPOCH).unwrap_or_default().as_millis();\n+            eprintln!(\n+                \"mptunnel_native_budget ts_unix_ms={} round={} phase={:?} delivered={} lost={} ordinary_lost={} records={} anonymous={} app_limited={} balance={} capacity={} next_balance={} next_capacity={} boundary={} high={}\",\n+                unix_ms, self.round_count, self.state, delivered, lost,\n+                self.loss_budget_epoch_loss(&epoch).unwrap_or(0), epoch.record_ids.len(),\n+                epoch.anonymous_lost, epoch.is_app_limited, current.balance, current.capacity,\n+                next.balance, next.capacity, self.compensated_loss_boundary(), round_high,\n+            );\n+        }\n \n         // Commit every cumulative baseline, record-consumption bit, journal tuple, and bucket state\n         // only after all checked arithmetic above succeeded.\n@@ -1725,7 +1736,19 @@ impl Bbr3 {\n             // One immutable decision tuple produces at most one lower-bound action. Raw authority\n             // takes precedence, while a still-active ProbeUP consumes the completed compensated\n             // decision through its deterministic beta-scaled target.\n+            let before = (self.bw_shortterm, self.inflight_shortterm, self.inflight_longterm);\n             self.adapt_lower_bounds_from_congestion(true);\n+            if std::env::var_os(\"MPTUNNEL_NATIVE_MODEL_TRACE\").is_some_and(|v| v == \"1\") {\n+                let unix_ms = std::time::SystemTime::now()\n+                    .duration_since(std::time::UNIX_EPOCH).unwrap_or_default().as_millis();\n+                eprintln!(\n+                    \"mptunnel_native_lower ts_unix_ms={} round={} phase={:?} budget={} raw={} explicit={} unknown={} cwnd={} inflight_latest={} bw_latest={} bw_before={} short_before={} long_before={} bw_after={} short_after={} long_after={}\",\n+                    unix_ms, self.round_count, self.state, budget_high, requires_raw_response,\n+                    self.explicit_congestion_in_round, self.loss_round_evidence_unknown,\n+                    self.cwnd, self.inflight_latest, self.bw_latest, before.0, before.1, before.2,\n+                    self.bw_shortterm, self.inflight_shortterm, self.inflight_longterm,\n+                );\n+            }\n         }\n         if budget_high {\n             self.apply_compensated_probe_response(now, budget_is_app_limited);\n@@ -2859,12 +2882,22 @@ impl Bbr3 {\n         tx_in_flight: u64,\n         is_app_limited: bool,\n     ) {\n+        let before = self.inflight_longterm;\n         self.prev_probe_too_high = true;\n         self.bw_probe_samples = false;\n         if !is_app_limited {\n             self.inflight_longterm =\n                 Ord::max(tx_in_flight, (self.target_inflight() as f64 * BETA) as u64);\n         }\n+        if std::env::var_os(\"MPTUNNEL_NATIVE_MODEL_TRACE\").is_some_and(|v| v == \"1\") {\n+            let unix_ms = std::time::SystemTime::now()\n+                .duration_since(std::time::UNIX_EPOCH).unwrap_or_default().as_millis();\n+            eprintln!(\n+                \"mptunnel_native_long ts_unix_ms={} round={} phase={:?} app_limited={} tx_in_flight={} target_inflight={} cwnd={} long_before={} long_after={}\",\n+                unix_ms, self.round_count, self.state, is_app_limited, tx_in_flight,\n+                self.target_inflight(), self.cwnd, before, self.inflight_longterm,\n+            );\n+        }\n \n         if self.state == BbrState::ProbeBw(ProbeBwSubstate::Up) {\n             let source = self\n@@ -3410,6 +3443,30 @@ impl Controller for Bbr3 {\n     }\n \n     fn metrics(&self) -> ControllerMetrics {\n+        // Temporary observation only: identify the active post-QoS window bound.\n+        // One controller per process is used in this diagnostic experiment.\n+        static TRACE: std::sync::OnceLock<Option<(Instant, std::sync::atomic::AtomicU64)>> =\n+            std::sync::OnceLock::new();\n+        if let Some((epoch, last)) = TRACE.get_or_init(|| {\n+            std::env::var_os(\"MPTUNNEL_NATIVE_MODEL_TRACE\")\n+                .is_some_and(|value| value == \"1\")\n+                .then(|| (Instant::now(), std::sync::atomic::AtomicU64::new(u64::MAX)))\n+        }) {\n+            let second = Instant::now().saturating_duration_since(*epoch).as_secs();\n+            if last.swap(second, std::sync::atomic::Ordering::Relaxed) != second {\n+                let unix_ms = std::time::SystemTime::now()\n+                    .duration_since(std::time::UNIX_EPOCH).unwrap_or_default().as_millis();\n+                eprintln!(\n+                    \"mptunnel_native_model ts_unix_ms={} phase={:?} ack_phase={:?} cwnd={} flight={} max_inflight={} inflight_short={} inflight_long={} bw={} max_bw={} bw_short={} min_rtt_us={} operational_rtt_us={} round={} cwnd_limited={} full_bw={} loss_high={} raw_loss_authority={} compensation={}\",\n+                    unix_ms, self.state, self.ack_phase, self.cwnd, self.inflight,\n+                    self.max_inflight, self.inflight_shortterm, self.inflight_longterm,\n+                    self.bw, self.max_bw, self.bw_shortterm, self.min_rtt.as_micros(),\n+                    self.inflight_rtt.as_micros(), self.round_count, self.is_cwnd_limited,\n+                    self.full_bw_reached, self.completed_loss_round_is_high,\n+                    self.completed_loss_round_raw_authority, self.loss_compensation_floor,\n+                );\n+            }\n+        }\n         ControllerMetrics {\n             congestion_window: self.window(),\n             ssthresh: None,\n",
  "reordering_instrumented_source": "//! Native loss tolerance learned from unambiguous late-original delivery.\n//!\n//! RFC 9002 §6.1 permits adaptive absolute time thresholds. Late delivery\n//! changes this detector, not congestion ownership: one original cannot undo\n//! the loss response for other packets. Aging follows RFC 8985 §6.2.\n\nuse crate::{Duration, Instant, TIMER_GRANULARITY};\n\nconst RECOVERY_PERSISTENCE: u8 = 16;\n\n#[derive(Clone, Copy, Debug, Default)]\npub(super) struct Reordering {\n    seen: bool,\n    observed_excess: Duration,\n    recovery_start: Option<Instant>,\n    recovering: bool,\n    remaining_recoveries: u8,\n}\n\nimpl Reordering {\n    pub(super) fn packet_threshold_enabled(&self) -> bool {\n        !self.seen\n    }\n\n    pub(super) fn loss_delay(&self, ordinary: Duration, current_rtt: Duration) -> Duration {\n        let selected = if self.observed_excess.is_zero() {\n            ordinary\n        } else {\n            ordinary.max(current_rtt.saturating_add(self.observed_excess))\n        };\n        // Temporary native deadline observation; never changes loss authority.\n        static TRACE: std::sync::OnceLock<Option<(Instant, std::sync::atomic::AtomicU64)>> =\n            std::sync::OnceLock::new();\n        if let Some((epoch, last)) = TRACE.get_or_init(|| {\n            std::env::var_os(\"MPTUNNEL_NATIVE_MODEL_TRACE\")\n                .is_some_and(|value| value == \"1\")\n                .then(|| (Instant::now(), std::sync::atomic::AtomicU64::new(u64::MAX)))\n        }) {\n            let second = Instant::now().saturating_duration_since(*epoch).as_secs();\n            if last.swap(second, std::sync::atomic::Ordering::Relaxed) != second {\n                let unix_ms = std::time::SystemTime::now()\n                    .duration_since(std::time::UNIX_EPOCH).unwrap_or_default().as_millis();\n                eprintln!(\n                    \"mptunnel_native_reordering ts_unix_ms={} ordinary_us={} current_rtt_us={} excess_us={} selected_us={} seen={} recoveries={}\",\n                    unix_ms, ordinary.as_micros(), current_rtt.as_micros(),\n                    self.observed_excess.as_micros(), selected.as_micros(), self.seen,\n                    self.remaining_recoveries,\n                );\n            }\n        }\n        selected\n    }\n\n    pub(super) fn on_late_original(&mut self, now: Instant, sent: Instant, current_rtt: Duration) {\n        // The caller matches only current-lineage, unexpired retained originals.\n        // Retain excess delay, not the common RTT component of an old queue.\n        // Recompose with the current baseline at loss detection. A genuinely\n        // large differential tail remains evidence; there is no RTT-sized cap.\n        let before = (self.observed_excess, self.remaining_recoveries);\n        self.seen = true;\n        self.observed_excess = self.observed_excess.max(\n            now.saturating_duration_since(sent)\n                .saturating_sub(current_rtt)\n                .saturating_add(TIMER_GRANULARITY),\n        );\n        self.remaining_recoveries = RECOVERY_PERSISTENCE;\n        if (before.0 != self.observed_excess || before.1 != self.remaining_recoveries)\n            && std::env::var_os(\"MPTUNNEL_NATIVE_MODEL_TRACE\").is_some_and(|v| v == \"1\")\n        {\n            let unix_ms = std::time::SystemTime::now()\n                .duration_since(std::time::UNIX_EPOCH).unwrap_or_default().as_millis();\n            eprintln!(\n                \"mptunnel_native_late ts_unix_ms={} age_us={} current_rtt_us={} excess_before_us={} excess_after_us={} recoveries_before={}\",\n                unix_ms, now.saturating_duration_since(sent).as_micros(), current_rtt.as_micros(),\n                before.0.as_micros(), self.observed_excess.as_micros(), before.1,\n            );\n        }\n    }\n\n    pub(super) fn on_loss(&mut self, now: Instant, sent: Instant) {\n        if self.recovery_start.is_none_or(|start| sent > start) {\n            self.recovery_start = Some(now);\n            self.recovering = true;\n        }\n    }\n\n    pub(super) fn on_ack(&mut self, sent: Instant) {\n        if self.recovering && self.recovery_start.is_some_and(|start| sent > start) {\n            self.recovering = false;\n            self.remaining_recoveries = self.remaining_recoveries.saturating_sub(1);\n            if self.remaining_recoveries == 0 {\n                self.observed_excess = Duration::ZERO;\n            }\n        }\n    }\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n\n    #[test]\n    fn no_proof_preserves_native_deadline_and_packet_detector() {\n        let now = Instant::now();\n        let mut state = Reordering::default();\n        state.on_loss(now, now - Duration::from_millis(100));\n        state.on_ack(now + Duration::from_millis(1));\n        assert!(state.packet_threshold_enabled());\n        let ordinary = Duration::from_micros(112_500);\n        assert_eq!(state.loss_delay(ordinary, Duration::from_millis(100)), ordinary);\n        // No evidence must preserve even an explicitly shorter native policy.\n        // MPP's default D0 is already >= RTT; this protects the owner boundary.\n        let short = Duration::from_millis(50);\n        assert_eq!(state.loss_delay(short, Duration::from_millis(100)), short);\n    }\n\n    #[test]\n    fn learned_deadline_covers_the_original_that_disproved_loss() {\n        let start = Instant::now();\n        let mut state = Reordering::default();\n        let elapsed = Duration::from_millis(180);\n        let base = Duration::from_millis(100);\n        state.on_late_original(start + elapsed, start, base);\n        assert_eq!(state.loss_delay(Duration::from_micros(112_500), base), elapsed + TIMER_GRANULARITY);\n        assert!(!state.packet_threshold_enabled());\n        // A burst of younger originals cannot inflate or erase the observed fact.\n        for n in 0..100 {\n            state.on_late_original(start + elapsed, start + Duration::from_micros(n), base);\n        }\n        assert_eq!(state.loss_delay(Duration::ZERO, base), elapsed + TIMER_GRANULARITY);\n        assert_eq!(state.loss_delay(Duration::from_millis(250), base), Duration::from_millis(250));\n    }\n\n    #[test]\n    fn adaptation_ages_by_completed_recoveries_not_loss_batches() {\n        let start = Instant::now();\n        let mut state = Reordering::default();\n        let base = Duration::from_millis(100);\n        state.on_late_original(start + Duration::from_millis(180), start, base);\n        for n in 0..16 {\n            let sent = start + Duration::from_secs(n + 1);\n            let loss_at = sent + Duration::from_millis(100);\n            state.on_loss(loss_at, sent);\n            state.on_loss(loss_at + Duration::from_millis(1), sent);\n            state.on_ack(sent);\n            assert_eq!(state.remaining_recoveries, 16 - n as u8);\n            assert!(state.observed_excess > Duration::ZERO);\n            state.on_ack(loss_at + Duration::from_millis(1));\n            state.on_ack(loss_at + Duration::from_millis(2));\n        }\n        assert_eq!(state.observed_excess, Duration::ZERO);\n        assert_eq!(state.loss_delay(Duration::from_micros(112_500), base), Duration::from_micros(112_500));\n        assert!(!state.packet_threshold_enabled());\n    }\n\n    #[test]\n    fn common_queue_delay_does_not_become_future_reordering_delay() {\n        let start = Instant::now();\n        let elapsed = Duration::from_micros(2_014_429);\n        let queued_base = Duration::from_micros(1_905_151);\n        let recovered_base = Duration::from_micros(54_912);\n        let mut state = Reordering::default();\n        state.on_late_original(start + elapsed, start, queued_base);\n        let expected = recovered_base + (elapsed - queued_base) + TIMER_GRANULARITY;\n        assert_eq!(state.loss_delay(recovered_base.mul_f32(1.125), recovered_base), expected);\n    }\n}\n"
}
